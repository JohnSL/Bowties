# Module Ownership

## Summary

| Layer | Count | Key shared logic |
|-------|-------|-----------------|
| Routes | 3 pages + layout | Screen composition, tab wiring |
| Components | ~40 across 9 dirs | Rendering, intent emission |
| Orchestrators | 15 | Async workflows, lifecycle transitions |
| Stores | ~22 | Durable frontend state, derived values |
| Utils | ~14 | Normalization, formatting, serialization |
| API | 9 | Tauri IPC bindings |
| bowties-core | 20 modules | Node tree, layout persistence, profile, registry, placeholder, bowtie catalog, channels |
| Backend (Tauri shell) | ~16 modules | IPC commands, state, placeholder factory, events |
| lcc-rs | ~20 modules | Protocol encoding, transport, CDI parsing |

Governing docs: `product/architecture/code-placement-and-ownership.md`, `product/architecture/frontend-boundaries.md`

---

## Routes (`app/src/routes/`)

| File | Purpose | Test |
|------|---------|------|
| `+page.svelte` | Main app page; tabs for layout/discovery/config/traffic. **3-tab strip (Config | Bowties | Railroad)** with `role="tablist"` / `role="tab"` / `aria-selected` a11y and ArrowLeft/Right cycling keyboard nav. **Picker gate (Spec 013 / S6):** when no layout is active (and bootstrap is finished and no open-in-progress), `+page.svelte` renders `LayoutPicker` instead of the toolbar + main-content; disconnecting does NOT re-show the picker (layout stays active). **Window-close lifecycle:** owns `onCloseRequested` handler — always prevents native close, shows unsaved-changes dialog when dirty, calls `disconnect_lcc` for graceful LCC teardown before exiting. Menu Exit follows the same path via `promptUnsaved`. | `page.route.test.ts` |
| `+layout.svelte` | Root layout wrapper | — |
| `+layout.ts` | Disables SSR (SPA-only for Tauri) | — |
| `config/+page.svelte` | Config editor; renders ConfigSidebar + ElementCardDeck | — |
| `traffic/+page.svelte` | Live CAN traffic monitor | — |

**Global font stack** lives in `+page.svelte`'s `:global(html, body)` block and is mirrored by `.toast-wrap` in `+layout.svelte`. Canonical stack: `'Segoe UI Variable', 'Segoe UI', system-ui, -apple-system, BlinkMacSystemFont, sans-serif` (Fluent v9). Many components redeclare the same stack locally to work around form-control non-inheritance (`<button>`, `<input>`, `<select>`, `<textarea>`); these are being retired as components migrate to the `Dialog/` shell — see [specs/018-block-indicator-facility/dialog-shell-refactor.md](../specs/018-block-indicator-facility/dialog-shell-refactor.md).

---

## Components (`app/src/lib/components/`)

### Bowtie/ — Event-centric connection UI
| File | Purpose | Test |
|------|---------|------|
| `BowtieCard.svelte` | Bowtie card with producer/consumer columns. Remove button hidden for slots whose node is offline (`nodeRoster.isOffline`). Offline badge shown via `ElementEntry` prop. | `BowtieCard.test.ts` |
| `BowtieCatalogPanel.svelte` | Full catalog panel with editable metadata. "+ New Connection" button disabled when all nodes are offline. | `BowtieCatalogPanel.test.ts` |
| `ElementEntry.svelte` | Event slot entry (producer or consumer). Accepts `isNodeOffline` prop to render ⚠ badge and faded styling. | — |
| `ElementPicker.svelte` | Searchable node/slot picker. Filters out offline nodes (`nodeRoster.isOffline`). | `ElementPicker.test.ts` |
| `PickerTreeNode.svelte` | Tree node in picker hierarchy | — |
| `EmptyState.svelte` | Empty catalog prompt | `EmptyState.test.ts` |
| `AddElementDialog.svelte` | **dialog-shell-refactor Slice 5:** picker for adding a producer or consumer element to an existing bowtie, wrapping the `Dialog/` shell. 520px wide; title contains a role badge (Producer / Consumer) and the bowtie name. Body is a fixed-height (`min(60vh, 520px)`) flex column so the inner `ElementPicker` scroll container sizes correctly. | — |
| `NewConnectionDialog.svelte` | **dialog-shell-refactor Slice 4:** dual element picker wrapping the `Dialog/` shell (replaced the prior native `<dialog>` + custom backdrop). 960px wide; body is a fixed-height (`min(72vh, 640px)`) flex column so the inner `ElementPicker` scroll containers size correctly. | — |
| `ConnectorArrow.svelte` | Visual arrow connecting producers to consumers | — |
| `RoleClassifyPrompt.svelte` | Sidecar prompt for resolving ambiguous event roles without hiding the slot row | — |

### ConfigSidebar/ — Node/section navigation
| File | Purpose | Test |
|------|---------|------|
| `ConfigSidebar.svelte` | Left-side nav; nodes and CDI segments | `ConfigSidebar.test.ts` |
| `configSidebarPresenter.ts` | Presenter logic for sidebar state: `buildSidebarNodeEntries` (derives `SidebarNodeEntry[]` including `isOffline` from `connection_status === 'NotResponding'`), `shouldShowConfigNotReadBadge`, `getNodePendingState`, `getSegmentPendingState` | `configSidebarPresenter.test.ts` |
| `NodeEntry.svelte` | Clickable node with status badge | `NodeEntry.test.ts` |
| `SegmentEntry.svelte` | Clickable CDI segment entry | `SegmentEntry.test.ts` |
| `ConnectorSlotSelector.svelte` | Daughterboard slot dropdown | — |
| `SidebarResizeHandle.svelte` | Drag handle for resizing the sidebar panel; emits `resize` events with clamped width. Keyboard accessible (arrow keys ±10px). | — |

### ElementCardDeck/ — Config value editing
| File | Purpose | Test |
|------|---------|------|
| `ElementCardDeck.svelte` | Container for top-level group cards | `ElementCardDeck.test.ts` |
| `ElementCard.svelte` | Single CDI group card | `ElementCard.test.ts` |
| `SegmentView.svelte` | All cards in a segment | `SegmentView.test.ts` |
| `TreeLeafRow.svelte` | Single config field row | `TreeLeafRow.test.ts`, `TreeLeafRow.offline.test.ts` |
| `TreeGroupAccordion.svelte` | Accordion for replicated groups | `TreeGroupAccordion.test.ts` |
| `SubGroupAccordion.svelte` | Nested non-root group accordion | — |
| `EventSlotRow.svelte` | Event ID slot field | — |
| `FieldRow.svelte` | Generic field row wrapper | — |
| `SaveControls.svelte` | Save/discard buttons for batch writes. **Spec 014 / Step 7 Option H:** thin delegate — only owns its local `saveProgress` UI state machine and invokes `onSave` / `onSaveAs` props. All cleanup wiring (drafts, metadata, mark-clean, offline reload) is owned by `saveLayoutOrchestrator` via callbacks composed in `+page.svelte`. The component MUST NOT reach into `layoutStore`, `bowtieMetadataStore`, `configChangesStore`, or `offlineChangesStore` directly. **Spec 018 / S1.2 (ADR-0011 extension 2026-06-28):** the toolbar derivation flows through `effectiveNodeStore.dirtyBreakdown` (via `deriveSaveControlsViewState`) instead of raw per-store reads, so facility-only and channel-only edits light up the toolbar; `handleConfirmDiscard` now invokes `facilitiesStore.discard()` alongside the other store discards (pattern parity). | `SaveControls.test.ts`, `saveControlsPresenter.test.ts` |

### Sync/ — Layout reconciliation
| File | Purpose | Test |
|------|---------|------|
| `SyncPanel.svelte` | Modal for resolving offline/online mismatches | `SyncPanel.display.test.ts`, `SyncPanel.lifecycle.test.ts` |
| `ConflictRow.svelte` | Conflict row with resolution controls | — |
| `CleanSummarySection.svelte` | Summary of clean (uncontended) rows | — |

### PillSelector/ — Instance selector
| File | Purpose | Test |
|------|---------|------|
| `PillSelector.svelte` | Dropdown pill for replicated group instances | `PillSelector.test.ts` |

### Layout/ — Offline mode indicators
| File | Purpose | Test |
|------|---------|------|
| `OfflineBanner.svelte` | Offline status banner with sync trigger | — |
| `MissingCaptureBadge.svelte` | Badge for missing snapshot values | — |

### LayoutPicker/ — Startup gate (Spec 013 / S6)
| File | Purpose | Test |
|------|---------|------|
| `LayoutPicker.svelte` | Fullscreen startup picker. Renders known-layout list + `New Layout` + `Browse…`. Emits `onOpen` / `onBrowse` / `onCreate` / `onRemove` — no async work owned here. | — |
| `LayoutEntry.svelte` | Single known-layout row with name, path, locale-formatted `lastOpened`, and a Remove (✕) action. | — |
| `NewLayoutDialog.svelte` | **dialog-shell-refactor Slice 4:** modal for creating a new layout, wrapping the `Dialog/` shell. Folder picker via `@tauri-apps/plugin-dialog`, sanitised folder name, preview of derived `<parent>/<name>` path. While `busy`, the shell locks (no Esc, overlay, or ×) via `closable={false}`. `zIndex={1600}` preserves the prior stacking. Submit data-testids preserved (`new-layout-name`, `new-layout-directory`, `new-layout-create`). | — |

### Railroad/ — Information channel inventory (Spec 015) + Live channel state (Spec 016)
| File | Purpose | Test |
|------|---------|------|
| `RailroadPanel.svelte` | **Spec 018 / S3 → S4 → S6:** outer Railroad-tab composer. Renders `<FacilitiesSection>` (Spec 018 / S1) above the new `<ChannelsPanel>`. Threads `nodeName`, `resolvedEventIds`, optional `daughterboardName(nodeKey, connector)`, plus (S4) the `usedBy(channelId)` resolver and the `onSelectChannel` / `onAddChannel` / `onRemoveFromSlot` slot-binding intent callbacks through to `ChannelsPanel` and `FacilitiesSection`. `+page.svelte` mounts only `<RailroadPanel>` on the Railroad tab (no longer a sibling `<FacilitiesSection>` mount). **S6 (2026-07-01, Phase R):** `onRebindChannel` pass-through removed \u2014 Rebind was retired in D4. | `RailroadPanel.integration.test.ts`, `page.route.test.ts` |
| `ChannelsPanel.svelte` | **Spec 018 / S3 → S4:** hardware-organised 6-column channels table grouped by node + subsystem (`TowerLCC-1 · Connector A · BOD-8`). Iterates `channelsStore.groupedByHardware`; emits one `<tr class="group-header">` per group followed by a `<ChannelRow>` per channel. Derives `channelStates` via `deriveChannelState` and the route-supplied `resolvedEventIds`. **S4 addition:** accepts a `usedBy(channelId)` resolver prop (supplied by the route from `effectiveLayoutStore.channelUsageMap`) and passes its result to each `<ChannelRow>`. No derivation in the component — pure pass-through. Empty-state guard renders inline (no channels yet). | `RailroadPanel.integration.test.ts` |
| `ChannelRow.svelte` | **Spec 018 / S3 → S4:** one channel = one `<tr>` with six cells (state-dot · name+ownership badge · role/style stack · location · state label · `Used by`). Click-to-rename name input matches the retired `ChannelCard` UX. `ownership` field drives the `HW` / `USER` badge. `binding.kind` discriminates location format (`Connector A · Input 1` vs `Row 7`). `usedBy?` array is populated by S4 from facility slot bindings via `effectiveLayoutStore.channelUsageMap` and rendered as `{facility} / {slot}` joined by `; ` — empty/undefined → em-dash. | `RailroadPanel.integration.test.ts`, `selectChannel.integration.test.ts` |

### Facilities/ — Facility cards and slot bindings (Spec 018)

| File | Purpose | Test |
|------|---------|------|
| `FacilitiesSection.svelte` | **Spec 018 / S1 → S4 → S6:** Railroad-tab section housing the Add-facility button + facility list. Owns its own `showAddDialog` state for `AddFacilityDialog`. **S4 additions:** accepts `resolvedEventIds`, `onSelectChannel`, `onAddChannel`, `onRemoveFromSlot` props from `RailroadPanel` and forwards them to each `<FacilityCard>`. **S6 (2026-07-01, Phase R):** `onRebindChannel` pass-through removed — Rebind was retired in S6 D4; swap a channel via Remove-from-slot + Select/Add. Mutates `facilitiesStore` only for facility-level CRUD (add/rename/delete); slot-binding intent bubbles up to the route (no direct `facilitiesStore.attachChannel` calls). | (covered by `RailroadPanel.integration.test.ts`, `selectChannel.integration.test.ts`) |
| `FacilityCard.svelte` | **Spec 018 / S1 → S4 → S6 (mockup-aligned):** one facility = one card with a header (title group: name · template label · filled status pill with pulse dot · Rename/Delete link actions) + a **horizontal slot grid** (`1fr 28px 1fr`) with `→` arrows between adjacent slots; single-slot templates collapse to one column via a `:has(...)` rule. Click-to-rename name input. **Status derivation (S6 D5, T16):** `status = effectiveLayoutStore.facilityStatus(facility.facilityId)` — component no longer re-derives from `slotBindings`; the facade is the sole reader. Computes the filled-state display per slot from `channelsStore.channels` + `eventStateStore.events` + the route-supplied `resolvedEventIds` (picks element 0 of the Vec since UI is max-1 today) and threads `channel.ownership` through `currentChannelDisplay.ownership` so the slot can render the `HW`/`USER` badge. Threads slot intent callbacks to each `<FacilitySlot>` and prepends `facilityId` before bubbling. **S6 (2026-07-01, Phase R):** `onRebindChannel` prop + pass-through removed (D4). | `FacilityCard.test.ts` |
| `FacilitySlot.svelte` | **Spec 018 / S1 → S4 → S6 (mockup-aligned):** one slot inside a facility card, rendered as a vertical card with an uppercase `{Label} (input)`/`{Label} (output)` header (kind suffix derived from `SlotDefinition.kind`: producer→input, consumer→output). Empty state (mockup §3): italic "empty" + right-aligned action button — role-branched into `Select channel…` (producer slots) vs. `Add channel…` (consumer slots, S5). Filled state (mockup §6): state dot · channel name · `HW`/`USER` ownership badge · `{groupLabel} · {locationLabel} · {state}` meta line + `Remove from slot` link action (no Rename per D6 — channel rename lives in the Channels panel; no Rebind per S6 D4 — swap = Remove + Select/Add). Accepts `currentChannelId?`, `currentChannelDisplay?` (with `ownership: 'hardware-owned' \| 'user-owned'`), plus `onSelectChannel(slot)` / `onAddChannel(slot)` / `onRemoveFromSlot(slot, currentId)` callbacks. Pure render + emit; no store reads/writes. **S6 (2026-07-01, Phase R):** Rebind button + `onRebindChannel` prop deleted. | (covered by `selectChannel.integration.test.ts`) |
| `SelectChannelPicker.svelte` | **Spec 018 / S4 (new), S5 update, S6 Phase R (Rebind retired):** modal picker for the Select-channel workflow (mockup §5). Built on the `Dialog/` shell (alertdialog, focus trap, Esc / overlay / × → onCancel). Props (post-S6): `{ slotLabel, requiredRole, candidateChannels, channelState, onConfirm, onCancel }`. Confirm enables the moment any candidate is selected — no rebind pre-select, no Confirm-disabled-vs-current gate, no title branch. **S5:** `channelState` prop returns `ChannelState` (D3 discriminated union); state-dot class derived via `channelStateClass`; supports lit/unlit literals even though the current Select-channel flow only handles block-occupancy. **S6 (2026-07-01, Phase R):** `currentChannelId` prop + Rebind title branch + `.scp-current-pill` CSS removed. The route owns picker open/close state and dispatches `onConfirm` through `facilityOrchestrator.selectChannelForSlot`. | `SelectChannelPicker.test.ts` |
| `AddChannelPicker.svelte` | **Spec 018 / S5 (new):** modal sub-picker for Add-channel on consumer-side output slots. Built on the `Dialog/` shell parallel to `SelectChannelPicker`. Props: `{ slotLabel, requiredRole: 'lamp-indicator', requiredStyle: 'single-led-direct-lamp', candidateGroups: Array<{ nodeKey, nodeName, rows: Array<{ nodeKey, nodeName, rowOrdinal, rowLabel }> }>, onConfirm(lampRowNodeKey, rowOrdinal), onCancel }`. Renders a search input filtering by node or row name (whole-group match when the search hits the node name), one **non-selectable section header per node** so the user can tell which Signal-LCC a row lives on, and a radio list of unclaimed Direct Lamp Control rows under each header (row labels reuse `getInstanceDisplayName` — `"Up Main Block 5 (7)"` when Lamp Description is set, else `"Lamp 7"`). The route supplies candidates from `effectiveLayoutStore.eligibleLampRowsForStyle('single-led-direct-lamp')` (returns groups) and dispatches `onConfirm` through `facilityOrchestrator.addChannelForSlot`. **Note (2026-06-30 quickchange):** the earlier `lamp-selection-deferral-note` footer was removed as over-fitted to `single-led-direct-lamp`; the "must set Lamp Selection manually" discoverability now lives only on `ChannelRow.svelte`'s consumer-row tooltip + `docs/user/`, and the profile-language question is tracked in `specs/backlog.md` under "Profile-declared user-configuration prerequisites for consumer channels". | (covered by `addChannel.integration.test.ts`) |
| `AddFacilityDialog.svelte` | **Spec 018 / S1:** modal for creating a new facility (template select + name). Wraps the `Dialog/` shell; Enter on the focused input submits the primary action. | — |

### Dialog/ — Fluent v9 dialog shell (canonical design language)

New shared shell + tokens, introduced by `specs/018-block-indicator-facility/dialog-shell-refactor.md` (Slice 2). Per-dialog migrations from the prior bespoke chrome land in subsequent slices.

| File | Purpose | Test |
|------|---------|------|
| `Dialog/Dialog.svelte` | The shell. Owns overlay, surface, focus trap, Esc / overlay-click / × → `onCancel`, header divider + filled footer. Props: `{ open, width?, closable?, role?, ariaLabel?, initialFocus?, onCancel, title?, children, actions? }`. Stays declarative — callers own workflow per `frontend-components.instructions.md`. | `Dialog/Dialog.test.ts` |
| `Dialog/DialogTitle.svelte` | Header content helper used inside `Dialog`'s `title` snippet. Optional `glyph='warning'|'error'|'info'` renders ⚠ / ❌ / ⓘ in Fluent palette (severeWarning orange for warning — glyph only, never on buttons). | — |
| `Dialog/DialogActions.svelte` | Right-aligned footer button cluster used inside `Dialog`'s `actions` snippet. Cancel-style first, primary last (Fluent v9). | — |
| `Dialog/Button.svelte` | Fluent v9 button. `appearance='primary'|'secondary'|'subtle'|'outline'` × `intent='default'|'danger'` × `size='sm'|'md'`. `primary + danger` is the destructive red used in confirms. Replaces the bespoke `.uc-btn--*`, `.dc-btn--*`, `.ed-btn--*`, etc. families as dialogs migrate. | `Dialog/Button.test.ts` |
| `Dialog/tokens.css` | Fluent v9 design tokens (`--fluent-*` prefix). Imported once in `+layout.svelte`. Additive — legacy `--accent-blue` / `--text-*` family untouched. | — |
| `Dialog/README.md` | Canonical authoring guide: when to use which variant, button-order convention, destructive-label rule, keyboard contract, what the shell does NOT do. **First stop for anyone authoring a new dialog.** | — |

### Root-level components
| File | Purpose | Test |
|------|---------|------|
| `ConnectionManager.svelte` | Add/edit/delete saved connections; connect to LCC network. Device presets with adapter-specific baud rate dropdowns (JMRI-aligned options), flow control per adapter, "Show additional settings" toggle for advanced presets (RR-CirKits, MERG). Supports TCP, GridConnect serial, MERG GridConnect serial (header encoding), SLCAN. Flow control options: None, RTS/CTS, XON/XOFF (only for "Other" presets). **dialog-shell-refactor Slice 4:** the Add/Edit modal now wraps the `Dialog/` shell (replaced the prior native `<dialog>` element). The connection list and "add saved connection" affordances remain in the component's main body, not the shell. | `ConnectionManager.test.ts` |
| `TrafficMonitor.svelte` | Live traffic frame display | — |
| `NodeList.svelte` | Discovered nodes with CDI viewer access | — |
| `NodeLabel.svelte` | Shared component for rendering node identity. Props: `parts: NodeDisplayParts`, `orientation: 'inline' | 'compact'`. Inline = one line (group headers, wide contexts). Compact = two stacked lines (sidebar). Model shown before manufacturer. Used by ConfigSidebar/NodeEntry, ChannelsPanel group headers, AddChannelPicker group headers, Bowtie/ElementEntry. | — |
| `ErrorDialog.svelte` | **dialog-shell-refactor Slice 3:** error modal wrapping the `Dialog/` shell. Passes `zIndex={2000}` so errors stay above other dialogs (preserves the prior stacking behavior). Glyph is ❌ (Fluent danger red). Has a secondary `Copy Error` button + inline copy-status feedback. Enter is intentionally not bound so users can select and copy text. Esc / overlay / × → close. | — |
| `AboutDialog.svelte` | **dialog-shell-refactor Slice 7:** Help → About modal, wrapping the `Dialog/` shell. Shows app name, version (from `@tauri-apps/api/app`), copyright, license, and a clickable GitHub link, all centered in the body. `initialFocus="last"` puts focus on the action Close button so Enter on the focused button closes the dialog (preserves the prior "Esc or Enter → close" contract). `zIndex={2000}` preserves the prior stacking. | — |
| `DiscardConfirmDialog.svelte` | **dialog-shell-refactor Slice 3:** confirm-modal for discarding unsaved edits, wrapping the `Dialog/` shell. Renders ⚠ + "Discard unsaved changes" title and a destructive-red "Revert" primary button. The verb "Revert" is preserved because `SaveControls.test.ts` queries for it and the consuming UI labels the trigger action with that verb. | — |
| `UnsavedChangesDialog.svelte` | **Spec 018 / S1.2 + dialog-shell-refactor Slice 2:** modal shown on open / close / disconnect / exit with unsaved edits. Wraps the `Dialog/` shell (no longer owns overlay, focus trap, Esc, or chrome). Props: `{ message, breakdown: DirtyBreakdown, confirmLabel, onConfirm, onCancel }`. Renders one line per non-zero bucket from `dirtyBreakdown`; zero-count buckets are suppressed. Cancel is the initial focus target (Fluent norm for destructive alertdialogs — the previous implementation focused Confirm). Confirm uses `appearance="primary" intent="danger"` (Fluent red `#c50f1f`), replacing the prior severeWarning orange `#ca5010`. All counts come from `effectiveNodeStore.dirtyBreakdown` (ADR-0011 extension 2026-06-28); the route captures the snapshot when the dialog opens. | `UnsavedChangesDialog.test.ts` |
| `AddBoardDialog.svelte` | Spec 014 / S8: modal picker for adding a placeholder board to the active offline layout. Lists bundled board-model profiles via `listBundledProfiles()` and calls `placeholderBoardOrchestrator.addPlaceholderBoard` on submit. Entry point is the native `File → Add Placeholder Board…` menu item (gated on `offlineActive && !busy`); mounted from `+page.svelte` behind a `showAddBoardDialog` state flag. **dialog-shell-refactor Slice 4:** wraps the `Dialog/` shell. While `submitting`, the shell suppresses Cancel via a no-op `onCancel` so the in-flight add can't be aborted mid-write. | — |
| `DiscoveryProgressModal.svelte` | Progress during discovery phases (reading, building-catalog, complete, cancelled) | — |
| `SaveProgressDialog.svelte` | **dialog-shell-refactor Slice 6:** modal during the three-phase save flow, wrapping the `Dialog/` shell. Reads phase + per-field bus-write counters from `saveProgressStore`. `closable={phase === 'error'}` — in-flight phases lock the shell (no Esc/overlay/×); only the failure state can be dismissed (Dismiss action button + Esc/×/overlay). Auto-dismisses on `complete`. | `saveProgress.svelte.test.ts` (store) |
| `RefreshButton.svelte` | Refresh discovered nodes | — |
| `CdiDownloadDialog.svelte` | **dialog-shell-refactor Slice 6:** CDI download prompt, wrapping the `Dialog/` shell with `glyph="info"` title. `closable={!downloading}` — Esc/overlay/× dismiss only before the download starts; once in flight the shell locks. Renders per-node spinner / ✓ / ✗ status indicators and a global "Downloading CDI… (N of M)" line. | `CdiDownloadDialog.test.ts` |
| `CdiXmlViewer.svelte` | **dialog-shell-refactor Slice 7:** syntax-highlighted CDI XML viewer, wrapping the `Dialog/` shell at `width="lg"` (large). Body is a fixed-height (`min(70vh, 600px)`) scroll container for the `<pre>` with Prism highlighting. Footer shows a contextual Copy button (only when XML is loaded) plus Close. Converted from pre-runes `export let` syntax to `$props()` + `$derived` during the migration. | — |
| `CdiRedownloadDialog.svelte` | **dialog-shell-refactor Slice 6:** compact dialog shown while re-downloading CDI for a single node via the "Re-download CDI" menu item. Wraps the `Dialog/` shell with `glyph="info"`. `closable={status === 'downloading' && !cancelling}` — Esc/overlay/× trigger Cancel only during in-flight download; failed and done states use explicit action buttons (Close + Retry / auto-close after 800 ms). | `CdiRedownloadDialog.test.ts` |
| `LayoutLoadingDialog.svelte` | **dialog-shell-refactor Slice 6 (extracted from `+page.svelte`):** non-dismissible "Opening layout" progress dialog. Wraps the `Dialog/` shell with `closable={false}` and `zIndex={2200}` (above `ErrorDialog`'s 2000). Body shows a spinner + status text fed from the route's `$layoutOpenStatusText`. | — |
| `ChannelRemovalDialog.svelte` | **dialog-shell-refactor Slice 3 (extracted from `+page.svelte`):** confirm-modal for discarding channels already wired to a slot when changing a daughter board. Props: `{ channelCount, onConfirm, onCancel }`. Spec 015 / S5 callsite is the connector-selection orchestrator. | — |
| `DeletePlaceholderDialog.svelte` | **dialog-shell-refactor Slice 3 (extracted from `+page.svelte`):** confirm-modal for deleting a placeholder board. Spec 014 / S8.5 / T11. The Delete button keeps `data-testid="confirm-delete-placeholder"` for integration-test selectors. | — |

---

## Orchestrators (`app/src/lib/orchestration/`)

| File | Purpose | Test |
|------|---------|------|
| `discoveryOrchestrator.ts` | Node discovery workflow: probe → SNIP/PIP → publish | `discoveryOrchestrator.test.ts` |
| `facilityOrchestrator.ts` | **Spec 018 / S4 (new) — Owner of the Slot Binding + User-Owned Channel Lifecycle seams.** Pure workflow module (no DOM, no IPC). **S4:** `selectChannelForSlot({ facilityId, slotLabel, channelId })` validates `channel.role === slot.requiredRole` via `behaviorTemplatesStore` and calls `facilitiesStore.attachChannel`. `removeFromSlot({ facilityId, slotLabel, channelId })` calls `facilitiesStore.detachChannel`. Throws `RoleMismatchError` on role mismatch (before any store write), `UnknownReferenceError` when the facility/slot/channel/template lookup misses, and `SlotAtMaxError` when the attach would exceed the template `maxChannels` cap. **S5 additions:** `addChannelForSlot({ facilityId, slotLabel, lampRowNodeKey, rowOrdinal, name? }): { channelId }` validates the slot's `requiredRole === 'lamp-indicator'`, derives `name ?? "${facility.name} ${slotLabel}"`, calls `channelsStore.createUserOwnedChannel` for the new `single-led-direct-lamp` channel, then immediately `facilitiesStore.attachChannel` — on attach failure rolls back the just-created draft. `removeFromSlot` now ALSO calls `channelsStore.removeUserOwnedChannel` when the bound channel is `ownership === 'user-owned'` (user-owned channel lifecycle = its binding; hardware-owned channels persist past detach). **S6 (2026-07-01, Phase R):** Rebind retired per D4 — `SelectChannelForSlotArgs` no longer carries `mode` or `previousChannelId`; the detach-then-attach branch is gone. `SlotAtMaxError` was promoted from unwritten intent to enforced up-front guard on both `selectChannelForSlot` and `addChannelForSlot` so the "Remove first, then Select/Add" contract fails loudly when misused (previously the store's docstring advertised the check as an orchestrator responsibility but no code enforced it, hidden by Rebind's implicit detach-first). Component layers MUST go through this orchestrator — they do not call `facilitiesStore.{attachChannel, detachChannel}` or `channelsStore.{createUserOwnedChannel, removeUserOwnedChannel}` directly. **S6 additions (2026-07-01, D2 + D6 + Facility Bowtie Lifecycle seam owner):** `composeBowtiesIfWired(facilityId)` guards on `effectiveLayoutStore.facilityStatus(id) === 'Wired'` (no-op otherwise), invokes the `composeFacilityBowties` IPC, and dispatches each `CompositionOp` through `configEditor.applyEdit(editKeyForLeaf(nodeKey, leaf.space, leaf.address), { type: 'eventId', hex: canonicalEventIdHex(op.eventIdBytes) })` + `bowtieMetadataStore.createBowtie(hex, name, { createdByFacility })`. `tearDownFacilityBowties(facilityId)` delegates its leaf reversal to the shared `resetComposedLeavesForFacility(facilityId)` primitive (2026-07-03 consolidation), then drops the paired metadata rows via `bowtieMetadataStore.deleteBowtie`. `resetComposedLeavesForFacility` picks between two lookup strategies based on current `facilityStatus`: **composer-forward** when the facility is still Wired (calls `composeFacilityBowties` and uses the returned ops' leaf paths — the pre-consolidation Wired-branch behaviour, unchanged for `removeFromSlot`); **metadata-driven fallback** when the facility is Incomplete (scans every `nodeTreeStore.trees` entry via `collectEventIdLeaves` for leaves whose effective value equals a `bowtieMetadataStore.bowtiesForFacility(facilityId)` hex, and resets each match). The fallback lets `_cascadeDetach` (runtime hardware-channel cascade, which detaches before teardown) and `reconcileDanglingChannelRefsOnLoad` (2026-07-03 load-time repair for ghost bindings) fully reverse composition instead of leaking orphan consumer leaves for the CDI-scan catalog to re-surface on the next open. `deleteFacility(facilityId)`: iterates the facility's bound slots and calls `channelsStore.removeUserOwnedChannel` for each user-owned channel (hardware-owned channels are left alone), then teardown → `facilitiesStore.deleteFacility`. `selectChannelForSlot` + `addChannelForSlot` post-attach both `await composeBowtiesIfWired(facilityId)`. `removeFromSlot` runs teardown BEFORE the detach so it sees the still-Wired shape. All mutations remain draft-layer; no new save-flow ordering. | `facilityOrchestrator.test.ts` |
| `facilityCascadeOrchestrator.svelte.ts` | **Spec 018 / S6 (new) — Owner of the Hardware Channel Cascade seam (D3).** Svelte module with a private `Set<string>` of last-seen channel ids and `startCascade()` / `stopCascade()` bracketing an `$effect.root` subscription to `channelsStore.channels`. On each transition it diffs the new id set against the previous; for each disappearing hardware-owned channel it (a) iterates `facilitiesStore.facilities` and calls `facilitiesStore.detachChannel(facilityId, slotLabel, lostChannelId)` for every slot referencing the lost id, then (b) for each facility whose `effectiveLayoutStore.facilityStatus` transitioned Wired → Incomplete calls `facilityOrchestrator.tearDownFacilityBowties(facilityId)`. All side effects land in the draft layer so they surface immediately in the toolbar's `dirtyBreakdown`, apply atomically on Save, and revert on Discard (ADR-0012 extension 2026-07-01). Mounted by `+page.svelte`'s layout-open path (alongside `loadFacilities()` / `loadChannels()`); torn down by `layoutLifecycleOrchestrator.resetForNewLayout()`. User-owned channels that vanish via `removeUserOwnedChannel` do NOT re-trigger the cascade meaningfully because `removeFromSlot` already handled them. **2026-07-03 extension:** exposes `reconcileDanglingChannelRefsOnLoad()`, a load-time repair pass that walks `facilitiesStore.facilities` and stages a `detachChannelFromSlot` draft (via the shared private `_cascadeDetach(lostIds)` helper) for every slot binding pointing at a channel id absent from `channelsStore.channels`. Called from `+page.svelte` after `Promise.all([channelsStore.loadChannels(), facilitiesStore.loadFacilities()])` resolves. Fixes the three-part user-visible bug where orphan bindings from earlier split-write channel-save regressions counted against the slot cap (blocking Add), kept facilities looking Wired against a phantom consumer (routing Delete through the backend composer, which errored), and never appeared as unsaved changes. | `facilityCascadeOrchestrator.svelte.test.ts` |
| `configReadOrchestrator.ts` | Per-node CDI read preflight and eligibility checks (pure helpers; composed by `configAcquisitionOrchestrator`) | `configReadOrchestrator.test.ts` |
| `configAcquisitionOrchestrator.svelte.ts` | **Stateful owner of the acquire-config workflow**: CDI preflight → missing-CDI download dialog → config reads → per-node progress → cancel. Class with `$state` + reactive getters; route instantiates with constructor-injected deps (`new ConfigAcquisitionOrchestrator({...})`) and delegates intent (`readRemaining`, `readSingleNode`, `cancel`, `downloadMissingCdi`, `cancelDownload`, `applyProgressEvent`). Merges newly cached nodes into `cdiCacheStore` (its single owner; S6). | `configAcquisitionOrchestrator.svelte.test.ts` |
| `configDraftOrchestrator.ts` | Offline persistence + tree-refresh reconciliation for config drafts. `stageDraftsForOfflineSave` copies current drafts into `offlineChangesStore` before an offline save; `reconcileDraftsAfterTreeRefresh` prunes drafts whose refreshed baseline now matches; `discardAllConfigDrafts` clears the layer. **2026-07-03 extension (ADR-0012):** the online-mirror role moved out to `configDraftMirrorOrchestrator.svelte.ts` — `flushDraftToBackend` still exists for the two legacy callsites (leaf-row edit commit + bowtie-catalog panel) and is retired in Commit 2 once the mirror has soaked. | `configDraftOrchestrator.test.ts` |
| `configDraftMirrorOrchestrator.svelte.ts` | **ADR-0012 2026-07-03 extension — sole owner of the Config Draft Backend Mirror seam.** Svelte module with a private `Map<string, TreeConfigValue>` of last-seen drafts and `startMirror()` / `stopMirror()` bracketing an `$effect.root` subscription to `configChangesStore.draftEntries()`. On every reactive tick it diffs the current draft snapshot against the last-seen map and emits `setModifiedValue(dottedNodeId, address, space, value)` for each new or changed draft. Silently skips emissions when `layoutStore.isConnected === false` (checked inside the effect body, NOT as a reactive dependency — the mirror does not re-emit backlog on connect) and when the NodeKey is a placeholder (no bus identity; `stageDraftsForOfflineSave` handles those at offline-save time). Removed keys produce NO IPC — baseline update / draft prune already reconciled the backend. Mounted by `+page.svelte`'s layout-open path alongside `facilityCascadeOrchestrator.startCascade()`; torn down by `layoutLifecycleOrchestrator.resetForNewLayout()`. Closes the "connected Save didn't write config values" bug where facility composition, teardown resets, load-time repair, and cascade side effects staged config drafts that `save_layout_with_bus_writes` Phase 2 never observed because no code invoked `flushDraftToBackend` on the composed leaves. The test seam `reconcile(entries)` drives the same diff pass without a mounted Svelte harness. | `configDraftMirrorOrchestrator.svelte.test.ts` |
| `cdiInspectionOrchestrator.svelte.ts` | **Stateful owner of CDI inspection**: read-only XML viewer + menu re-download dialog. Class with `$state` + getters; route instantiates with injected deps and delegates `openViewer`/`closeViewer`/`openRedownload`/`closeRedownload`. Viewer load (cache → download fallback → error) hidden behind `openViewer`; `loadCdiViewerState` exported as a pure helper. Does NOT read config values. | `cdiInspectionOrchestrator.svelte.test.ts` |
| `connectorSelectionOrchestrator.ts` | Connector slot selection + compatibility recompute + channel auto-create/remove (Spec 015 / S3+S5: step 4 removes old channels for slot then creates new from board `channelInputs` metadata). Exports pure `buildAutoCreatedChannels(profile, document, nodeName)` and `buildAutoCreatedChannelsForSlot(profile, document, nodeName, slotId)` for testability. **Spec 018 / S2 (ADR-0013):** the pure builders populate the new `{role, style, ownership, binding}` schema; `style` is required on each `channelInputs` entry (throws on missing) and `role` is mapped via a private `mappingToRole(mapping.channelType)` switch that throws on unknown slugs. Slot-removal filter reads from `binding.connectorInput` instead of the retired `hardwareRef`. | `connectorSelectionOrchestrator.test.ts` |
| `eventStateOrchestrator.ts` | **Spec 016 / S1:** Subscribes to `lcc-event-state` Tauri events, records them in `eventStateStore`. Also exposes `resolveChannelEventIds()` which batch-calls the backend IPC to map channels → event IDs using cached config trees + profile eventMapping. Returns teardown that unlistens + clears store. **Spec 017 / S1:** the route's resolve `$effect` now reactively reads `nodeRoster.liveEntries.length` (in addition to `channelsStore.channels` and `nodeTreeStore.trees.size`) so resolution re-runs each time a live proxy is registered — saved nodes light up indicators on connect without any CDI read because `register_node` seeds the proxy with the saved tree. **Spec 018 / S2 (ADR-0013):** `resolveChannelEventIds(channels)` is style-aware — no per-call `eventMapping` argument; it derives the mapping per channel by looking up `channelStyles.getStyleEventMapping(ch.style)`. Channels whose binding is not `connectorInput` or whose style is unknown are silently skipped. | `eventStateOrchestrator.test.ts`, `page.route.test.ts` |
| `placeholderBoardOrchestrator.ts` | Spec 014 / S8.10: placeholder board lifecycle. Calls `addPlaceholderBoardIpc(profileStem)` (backend factory mints UUID, builds proxy, registers in backend), then reads tree via `getNodeTree` IPC and seeds `nodeRoster`. Delete gates on caller-supplied `confirm: () => Promise<boolean>` (FR-017a). UUID minting moved to backend factory in S8.10. | `placeholderBoardOrchestrator.test.ts` |
| `offlineLayoutOrchestrator.ts` | Offline layout hydration and snapshot replay. Exposes `buildOfflineDiscoveryNodes`, `buildOfflineTreesFromSnapshots`, `rehydrateOfflineStateFromSnapshots`, `restoreRecentOfflineLayout`, `openOfflineLayoutWithReplay`. The legacy standalone `resetLayoutStateForNoLayout` / `resetFreshLiveSessionState` exports remain for back-compat but new code MUST go through `layoutLifecycleOrchestrator` (ADR-0011) — the route's two wrappers now delegate. | `offlineLayoutOrchestrator.test.ts` |
| `layoutLifecycleOrchestrator.ts` | **Single owner of in-memory layout-lifecycle resets** (ADR-0011). Exports `LayoutScopedParticipant` interface (optional `resetForNewLayout()` and `resetForFreshLiveSession()` methods) and `layoutScopedParticipants` registry array. Every layout-scoped store or orchestrator implements the interface and is registered in the array; the orchestrator dispatches via loop instead of manual enumeration. Three named entry points: `resetForNewLayout({connected, reprobeLiveNodes, probeForNodes, afterReset})` (full teardown — close, discard, new-layout, no-layout recovery), `resetForFreshLiveSession()` (disconnect/reconnect within the same live session; preserves placeholders because they are layout-scoped), and `closeLayout({activeMode, closeLayoutIpc, clearRecentLayout, disconnectBeforeClose?, ...})` which sequences backend close → optional bus disconnect → frontend reset. The `configReadStatusParticipant` wraps the standalone `clearConfigReadStatus()` function. | `layoutLifecycleOrchestrator.test.ts` |
| `saveLayoutOrchestrator.ts` | Full save lifecycle: flush pending → persist layout → rebuild catalog → update context & partial nodes → clear metadata → mark clean. **S8.11:** accepts `inMemorySnapshotKeys?: string[]` (unified real-node + placeholder keys) and appends `{type:'addNode', nodeKey}` deltas; surfaces `persistedNodeIds` into the updated `ActiveLayoutContext.layoutNodeIds`. | `saveLayoutOrchestrator.test.ts` |
| `startupOrchestrator.ts` | Layout picker lifecycle (Spec 013 / S6): `loadKnownLayouts`, `openLayoutFromRegistry` (open existing → register), `createNewLayout` (capture → save → reopen → register), `removeKnownLayout`, `deriveLayoutNameFromPath`. Pure functions with injected callbacks — no direct store or IPC imports. Registry refresh failures are non-fatal (logged via `onError`). | `startupOrchestrator.test.ts` (14 tests) |
| `syncSessionOrchestrator.svelte.ts` | **Stateful owner of the connect/disconnect workflow + sync-session lifecycle.** Class with `connectionLabel` `$state` + getter and injected `SyncSessionConnectionDeps`; route instantiates and delegates `connect(config)` / `disconnect()` / `disconnectBeforeLayoutSwitch()`. Connect/disconnect sequencing stays in the exported pure helpers `connectLiveSession` / `disconnectWithOfflineFallback` (composed by the class). `connected` is NOT owned here — `layout.svelte.ts` is authoritative (`setLayoutConnected` dep). `errorMessage` is NOT owned here — it is the page-wide banner, written by several workflows and reported through the narrow `setErrorMessage` dep. Also owns sync auto-trigger (`scheduleAutoSync`, `maybeTriggerSync`, `forceSyncPanel`) and `bootstrapStartupLifecycle` (`setConnectionLabel` setter used by startup-connected). | `syncSessionOrchestrator.svelte.test.ts` |
| `syncApplyOrchestrator.ts` | Post-apply reconciliation: rebuild offline trees | `syncApplyOrchestrator.test.ts` |
| `syncPanelViewOrchestrator.ts` | Sync panel user interactions (mode, deselect, apply) | `syncPanelViewOrchestrator.test.ts` |
| `lifecycleTransitionMatrix.ts` | App lifecycle transition decision logic | `lifecycleTransitionMatrix.test.ts` |
| `unsavedChangesGuard.ts` | **Removed (Spec 018 / S1.2):** `effectiveNodeStore.isDirty` + `dirtyBreakdown` are now the only source for the close-prompt decision; `+page.svelte` reads them directly. | — |
| `menuListeners.ts` | **Owner of the native-menu (`menu-*`) event listener lifecycle** (S4). `registerMenuListeners(actions, listenFn?)` registers one Tauri `listen` per entry in the `MENU_EVENT_BINDINGS` table and returns a single combined teardown; `listenFn` is injectable for tests. The route supplies each action body (store access + unsaved-changes guards) via `MenuActionHandlers`; this module owns only the event-name table and register/unlisten bookkeeping. Mirrors `installMenuShortcuts`' shape. | `menuListeners.test.ts` |

---

## Stores (`app/src/lib/stores/`)

| File | Purpose | Test |
|------|---------|------|
| `bowties.svelte.ts` | `bowtieCatalogStore` singleton + `buildEffectiveBowtiePreview()` (the catalog×tree×metadata×layout merge). The merge is consumed only by `$lib/layout/effectiveLayoutStore`; components do not import from here directly. | `bowties.svelte.test.ts` (tests exercise the merge through `effectiveLayoutStore`) |
| `bowtieMetadata.svelte.ts` | Pending bowtie name/tag/role edits; `collectDeltas()` converts edits to `LayoutEditDelta[]` for save. **Spec 018 / S6 (D1):** `createBowtie(eventIdHex, name?, opts?: { createdByFacility? })` propagates the optional back-reference through pending `create` edits + `collectDeltas`'s `LayoutEditDelta::createBowtie` emitter; `_getEffectiveMetadata` preserves it across hydrate + rename + delete + `adoptEventId` re-keys. New reader `bowtiesForFacility(facilityId): string[]` returns event-id-hex list of bowties whose effective metadata has `createdByFacility === facilityId` (baseline + pending create − pending delete). Owner of the composed-bowtie side of the **Facility Bowtie Lifecycle** seam — `facilityOrchestrator.composeBowtiesIfWired` calls `createBowtie(..., { createdByFacility })` and `tearDownFacilityBowties` reads `bowtiesForFacility(id)` to know what to drop. | `bowtieMetadata.svelte.test.ts` |
| `nodeTree.svelte.ts` | Unified node config tree (CDI + addresses + values) | `nodeTree.store.test.ts` |
| `configChanges.svelte.ts` | Layered change state (draft/offlinePending/baseline). `set()` guards against no-op drafts: skips/removes the draft when the new value equals the effective value (offlinePending or baseline). S8.12: `commitForSave()` replaces `clearNonPlaceholderDrafts()` — uniform draft clearing post-save. | `configChanges.test.ts` |
| `configEditor.svelte.ts` | Entry point for user-initiated config edits | `configEditor.test.ts` |
| `configFocus.svelte.ts` | Navigation request: bowtie → config field | `configFocus.test.ts` |
| `configReadStatus.ts` | Tracks nodes with successful config reads | `configReadStatus.test.ts` |
| `layout.svelte.ts` | Layout file state: open/save/save-as/recent; `hydrateFromBackend()` sets layout from backend response. **ADR-0011:** the per-node `isDirty` projection (fully-captured live nodes + persistability) is owned by `effectiveNodeStore`. `layoutStore.isDirty` carries only the LayoutFile-struct edits (`_hasFileEdits`); aggregate dirty signal lives on `effectiveNodeStore.isDirty`. Consumers (`SaveControls` presenter, top-bar dot, unsaved-changes count) MUST read through the facade, not directly off `layoutStore.isDirty`. | `layout.svelte.test.ts` |
| `layoutOpenLifecycle.ts` | Phase machine for layout open (opening→hydrating→ready) | — |
| `offlineChanges.svelte.ts` | Offline change row tracking (persisted/draft layers) | `offlineChanges.store.test.ts` |
| `syncPanel.svelte.ts` | Sync session state: conflict/clean row tracking | `syncPanel.store.test.ts` |
| `connectorSelections.svelte.ts` | Connector slot selections per-node. **Spec 014 / S6, ADR-0012:** in-memory draft layer — selections are held locally until save, when `collectDeltas()` produces `SetNodeModeSelection` / `ClearNodeModeSelection` deltas for `save_layout_directory`. Identity mapping for Tower-LCC: slot_id ≡ mode_id, daughterboardId ≡ variantId. `fromNodeModeSelections(nodeId, profile, selections)` projects the unified map back onto the slot list for display. Selector changes are wrapped by `connectorSelectionOrchestrator.applyConnectorSelectionChange` which awaits `nodeTreeStore.refreshTree(nodeId)` so the backend re-runs `annotate_tree` with the new selection and the rendered tree re-shapes. | `connectorSelections.s6.test.ts` |
| `partialCaptureNodes.svelte.ts` | Canonical `NodeKey` set of live nodes whose config read finished with at least one missing/failed leaf (partial capture warnings). Promoted from a `+page.svelte` `$state<Set>` so `effectiveNodeStore` and `layoutLifecycleOrchestrator` share a single reactive source for the partial-capture half of the full-capture threshold (ADR-0007, ADR-0011). Exposes `has(key)`, `replace(keys)`, `clear()`. | — |
| `cdiCache.svelte.ts` | Node-ID set of nodes whose CDI is present in the local cache. Promoted from a `+page.svelte` `$state<Set>` (S6) so the shared cached-CDI state has one owner. Written by `configAcquisitionOrchestrator` (preflight + post-download merge via `add`) and the refresh reconciler (`replace`); read by the native-menu enable effect (`has`) to gate "View CDI". Exposes `nodes`, `has(id)`, `add(ids)`, `replace(ids)`, `reset()`. | `cdiCache.svelte.test.ts` |
| `bowtieFocus.svelte.ts` | Currently focused bowtie card (keyboard nav) | — |
| `connectorSlotFocus.svelte.ts` | Focused connector slot per-node | — |
| `connectionRequest.svelte.ts` | Cross-tab connection request (config→bowtie) | — |
| `saveProgress.svelte.ts` | Phase tracker for the three-phase save flow. Listens to `save-progress` Tauri events (`saving-layout` / `writing-config` / `reconciling` / `complete`) emitted by `save_layout_with_bus_writes` + per-iteration events from `write_modified_values`. Also exposes `begin()` / `apply()` / `fail()` / `reset()` so the offline-save path (driven by `+page.svelte`) can flip phases without backend events. Consumed by `SaveProgressDialog.svelte` and by `isMenuBusy()` to gate concurrent saves. | `saveProgress.svelte.test.ts` |
| `eventState.svelte.ts` | **Spec 016 / S1:** Transient, channel-unaware event ledger. Records every PCER event received from the LCC bus as `eventId (hex) → timestamp (epoch-ms)`. Session-scoped. Exposes `record()`, `lastSeen()`, `events` (reactive Map), `clear()`. No draft layer (not persisted). **S2:** `clear()` is invoked from three lifecycle seams: `layoutLifecycleOrchestrator.resetForFreshLiveSession()` (connect, no-layout reset) and the `clearLiveState` / `rehydrateOffline` lambdas in `+page.svelte` (both disconnect branches). The PCER listener itself stays subscribed across disconnect (always-listening); only the ledger is cleared. | `eventStateOrchestrator.test.ts`, `layoutLifecycleOrchestrator.test.ts`, `page.route.test.ts` |
| `nodes.ts` | Global discovered nodes list (Svelte 5 runes) | — |
| `nodeInfo.ts` | nodeId → DiscoveredNode map for display-name resolution. **Spec 014 / S8.7:** kept as internal backing storage; the canonical single source of truth for "the set of nodes the user sees" is now `nodeRoster.svelte.ts`. New consumers should read from the roster facade. | — |
| `nodeRoster.svelte.ts` | **Spec 014 / S8.7, S8.12:** Unified facade over `nodeInfoStore`, `configReadNodesStore`, `nodeTreeStore`, and an internal `_profileStems` map (S8.12 — previously `inMemoryPlaceholdersStore`, now deleted). Exposes `allEntries` / `liveEntries` / `placeholderEntries` / `liveNodes` / `hasAnyEntries` / `has(nodeKey)` / `isOffline(nodeKey)` as reactive views/queries, and `upsertLive`, `replaceLiveRoster` (preserves placeholders), `addPlaceholder`, `removePlaceholder`, `markPlaceholdersPersisted`, `setTree`, `markRead`, `clearLayoutScope`, `injectOffBusSavedNodes` (synthesizes `NotResponding` entries for saved layout nodes absent from bus) as mutators. `isOffline` is the canonical predicate for "should this node be treated as read-only" — consumed by `ElementPicker`, `BowtieCatalogPanel`. | `nodeRoster.svelte.test.ts` |
| `pillSelection.ts` | Replicated group instance selections | `pillSelection.test.ts` |
| `configSidebar.ts` | Sidebar UI state: selected node/segment, expanded nodes, card deck. `reset()` clears all; `pruneToAvailableNodes(keys)` keeps selection for surviving nodes and clears transient state (card deck, loading states, errors). | `configSidebar.test.ts` |
| `sidebarWidth.ts` | App-wide sidebar panel width with localStorage persistence (`bowties:sidebarWidth`). Exposes `setWidth(px)` (clamped 160–600), `reset()`. Consumed by the config tab in `routes/+page.svelte` via the `--config-sidebar-width` CSS custom property on `.config-layout`; written by `SidebarResizeHandle.svelte` resize events. | `sidebarWidth.test.ts` |
| `traffic.ts` | Live traffic message stream | — |
| `knownLayouts.svelte.ts` | `knownLayoutsStore` singleton — frontend mirror of `known-layouts.json` (Spec 013 / S6). Exposes `entries`, `loaded`, `busy`; setters tolerate undefined backend payloads. Entries store layout folder paths. Written through by `startupOrchestrator`; read by `LayoutPicker`. | — |
| `channels.svelte.ts` | `channelsStore` singleton (Spec 015). Holds `InformationChannel[]` for the active layout. Exposes `channels`, `isEmpty`, `grouped` (by **role** per Spec 018 / S2), `groupedByHardware` (**Spec 018 / S3** — by node + subsystem via the module-level `hardwareGroupKey(channel)` helper; `connectorInput` channels group per `${nodeKey}|connector:${connector}`, `lampRow` channels group per `${nodeKey}|direct-lamp-control`), `loading`, `isDirty`, `editCount`, `pendingCreations`, `pendingRenames`, `pendingDeletions`; mutators `loadChannels()` (IPC), `addPendingChannels(ch[])`, `createUserOwnedChannel({role, style, binding, name})` (returns the fresh channel), `removeUserOwnedChannel(id)`, `deleteChannels(ids[])`, `renameChannel(id, newName)` (rejects empty; returns bool), `setChannels(ch[])`, `discard()`, `hydrateBaseline(ch[])`, `reset()`, `collectDeltas(): LayoutEditDelta[]`. Draft layer (ADR-0012): tracks a single `_pendingCreations` bucket for both hardware- and user-owned drafts (`ownership` is intrinsic to `InformationChannel`) plus `_pendingRenames` and `_pendingDeletions`; `channels` getter filters deletions and applies renames over baseline + creations. Hydrated on layout open (eager); reset on layout close via `layoutLifecycleOrchestrator`. **Spec 018 / S1.2 (ADR-0011 extension 2026-06-28):** `editCount` feeds `effectiveNodeStore.dirtyBreakdown.channels` so the Save / Discard toolbar and the close-prompt dialog react to channel-only edits. **Spec 018 / S2 (ADR-0013):** `InformationChannel` now carries `{role, style, ownership, binding}` instead of `{channelType, hardwareRef}`; `grouped` keys by `role`. **Atomic-save fold (2026-07-03, ADR-0002 extension):** the S5 split-bucket layout (`_pendingCreationDeltas`) was collapsed into `_pendingCreations`; `collectDeltas()` now emits `createChannel` / `renameChannel` / `deleteChannel` deltas covering the full CRUD lifecycle. All channel edits ride `save_layout_directory` in one atomic delta list. | `channels.svelte.test.ts`, `channels.s2.integration.test.ts` |; no auto-write of Lamp Selection yet. | `channels.svelte.test.ts`, `channels.s2.integration.test.ts` |
| `facilities.svelte.ts` | `facilitiesStore` singleton (Spec 018 / S1 → S4). Full ADR-0012 draft-layer contract: `facilities`, `isEmpty`, `isDirty`, `editCount`, `pendingCreations`, `pendingRenames`, `pendingDeletions`, **`pendingSlotBindings: Map<facilityId, Map<slotLabel, string[]>>`** (S4); mutators `addFacility(template, name)` (materialises empty slot bindings from template as `[]` per D8), `renameFacility(id, newName)` (no-op suppression against effective view; reverts pending rename when user matches baseline), `deleteFacility(id)` (removes pending creation OR records baseline deletion; also drops slot-binding overrides), `loadFacilities()`, `discard()`, `hydrateBaseline(facilities)`, `reset()`. **S4 additions:** `attachChannel(facilityId, slotLabel, channelId): boolean` and `detachChannel(facilityId, slotLabel, channelId): boolean` with no-op suppression (return `false` when the requested change is already reflected; return `true` when the override is staged or collapses back to baseline). **`collectDeltas(): LayoutEditDelta[]`** producing `addFacility / renameFacility / deleteFacility` (S1) plus `attachChannelToSlot / detachChannelFromSlot` (S4) variants. For pending-creation facilities, slot bindings travel inside the `addFacility` delta (no separate attach delta emitted); for baseline facilities, the override Vec is diffed against the baseline as a set and each net new/removed channel id becomes one delta. Hydrated on layout open in `+page.svelte`; reset on layout close via `layoutLifecycleOrchestrator`. **Spec 018 / S1.2:** `editCount` feeds `effectiveNodeStore.dirtyBreakdown.facilities` (now counts slot-binding edits too); `SaveControls.handleConfirmDiscard` calls `facilitiesStore.discard()` alongside the other store discards. | `facilities.svelte.test.ts` |
| `behaviorTemplates.svelte.ts` | `behaviorTemplatesStore` singleton (Spec 018 / S1 → S4). **Application-scoped** (NOT layout-scoped per D4): loaded once at app start via `loadBehaviorTemplates()` IPC; never enrolled in `layoutLifecycleOrchestrator.resetForNewLayout()`. Read-only mirror of the backend `bowties_core::behavior_templates` registry. Exposes `templates`, `loaded`, `findByTemplateId(id)`; `reset()` exists for tests only. **S4 addition:** the mirrored `SlotDefinition` now carries `minChannels: number, maxChannels: number | null` (D8 cardinality contract); the orchestrator looks up `requiredRole` here for role-match validation. | (covered by `facilities.svelte.test.ts` integration + `facilityOrchestrator.test.ts`) |

---

## Layout facade (`app/src/lib/layout/`)

**Public import surface for all layout state reads/writes (ADR-0004).** Components and routes should import only from `$lib/layout`; the underlying stores (`bowtieCatalogStore`, `bowtieMetadataStore`, `configChangesStore`, `layoutStore`) are internal collaborators of the facade and its orchestrator.

| File | Purpose | Test |
|------|---------|------|
| `index.ts` | Public facade. Re-exports `effectiveLayoutStore`, `effectiveNodeStore` + `DirtyBreakdown` type (Spec 018 / S1.2), `collectAllSaveDeltas` (2026-07-03 atomic-save fold, ADR-0002 extension), `bowtieCatalogStore`, `makeValueResolver`, `resolveNodeName`, `buildElementSelection`, `saveLayoutOrchestrated` + types, and the edit-recording commands (`recordBowtieDeletion`, `recordRoleClassification`, `recordConfigDraft`). `resolveNodeName(key: NodeKeyInput)` is the canonical single entry point for edit-layer-aware node Display Name resolution (ADR-0003 + ADR-0010). Dispatches live vs placeholder variants. `buildElementSelection(leaf, nodeId: NodeKeyInput)` is the canonical constructor for `ElementSelection` — resolves node name and element label through the edit layer in one call. | — |
| `effectiveLayoutStore.svelte.ts` | Single read model that merges all edit layers into the user-visible **value** view. `preview` / `effectiveBowties` (catalog × tree × metadata × layout, with `hasPendingDeletion` filter); `effectiveRole(nodeId, leaf)` (pending classify → catalog → leaf baseline); `effectiveValue(nodeId, leaf)` (draft override → leaf baseline); `slotsByRole(nodeId, role)`; `isSlotFree(nodeId, leaf)`; `usedInMap`. Composes `buildEffectiveBowtiePreview()` from `bowties.svelte.ts`. **Spec 018 / S4 (D1, D2):** also owns `channelUsageMap: Map<channelId, ChannelUsageEntry[]>` (single-merge derivation per ADR-0004 — flattens every facility's `slotBindings` into a per-channel `{facilityId, facilityName, slotLabel}[]`) and `unboundChannelsForRole(role, { excludeIds? })` (one-slot-per-channel invariant filter, reused by Select-channel + S5 Add-channel sub-picker). **S5 (D1):** `eligibleLampRowsForStyle(styleId, { excludeChannelId? })` returns `EligibleLampRowGroup[]` (rows grouped by node) — enumerates CDI `Direct Lamp Control/Lamp` replicated instances via `replicationInstances`, labels each row via `getInstanceDisplayName(instance, makeValueResolver(nodeId))` (Lamp Description → `"My Block 5 (7)"`; fallback → `"Lamp 7"`). Quickchange 2026-06-30 fixed the wrapper-traversal bug and switched to grouped output. **Spec 018 / S6 (D5):** new `facilityStatus(facilityId): 'Wired' \| 'Incomplete'` derivation — `'Wired'` iff every slot in the facility's template has `slotBindings[label].length >= slot.minChannels`. Unknown facility ids → `'Incomplete'` (defensive default); slots declaring `minChannels === 0` stay Wired when empty (forward-compat with future optional slots). Sole reader for the `FacilityCard` status pill — no component re-derives from raw `slotBindings`. | `effectiveLayoutStore.svelte.test.ts`, `channelUsage.test.ts`, `eligibleLampRows.test.ts`, `facilityStatus.test.ts` |
| `effectiveNodeStore.svelte.ts` | **Per-node** layout facade sibling to `effectiveLayoutStore` (ADR-0011, Step 10; extended Spec 018 / S1.2). Projects `nodeTreeStore`, `nodeInfoStore`, `configReadNodesStore`, `partialCaptureNodesStore`, `layoutStore.activeContext`, and the edit-layer stores into `nodeOrigin(key)`, `isFullyCaptured(key)`, `isConfigRead(key)`, `isPersistableInLayout(key)` (= `isFullyCaptured ∧ (isConfigRead ∨ placeholder)` — R5 fix), `unsavedInMemoryNodeIds`, `unsavedRemovedNodeIds`, `isDirty` (R6 fix), and `dirtyBreakdown: DirtyBreakdown` (S1.2). `dirtyBreakdown` is the typed snapshot of every edit-bearing store (`config`, `configNodes`, `metadata`, `channels`, `facilities`, `connectorSelections`, `offlineDrafts`, `offlineRevertedPersisted`, `layoutStruct`, `unsavedNewNodes`, `unsavedRemovedNodes`); `isDirty` is derived as "any bucket > 0". **Single-owner rule (ADR-0011 extension 2026-06-28):** every new edit-bearing store MUST be wired into `dirtyBreakdown` in the same slice it lands. Reads only — never writes through. The lifecycle reset path is `layoutLifecycleOrchestrator`, which MUST enumerate every store this facade reads. | `effectiveNodeStore.svelte.test.ts`, `dirtyAggregate.integration.test.ts` |
| `collectSaveDeltas.ts` | **Save-side sibling of `effectiveNodeStore.dirtyBreakdown`** — the single aggregator over every edit-bearing store for `LayoutEditDelta`s heading into `saveLayoutDirectory` (ADR-0002 extension 2026-07-03, atomic-save fold). Exports `collectAllSaveDeltas(): LayoutEditDelta[]` which dispatches via the `layoutScopedParticipants` registry — every store implementing `collectDeltas()` on the `LayoutScopedParticipant` interface participates automatically. Currently: `bowtieMetadataStore`, `connectorSelectionsStore`, `facilitiesStore`, `channelsStore`. **Single-enrolment rule:** adding a new edit-bearing store means implementing `collectDeltas()` on the `LayoutScopedParticipant` interface (already registered for lifecycle resets) AND adding a bucket in `dirtyBreakdown`. Consumed by `+page.svelte`'s `saveCurrentCaptureToFile`; consumers must not build their own delta lists from raw stores. | `collectSaveDeltas.test.ts` |

### Layout facade conventions

- **Single import surface.** New components MUST import layout reads via `$lib/layout`. Direct imports from `$lib/stores/bowties.svelte`, `$lib/stores/bowtieMetadata.svelte`, `$lib/stores/configChanges.svelte`, or `$lib/stores/layout.svelte` from components/routes are a code smell that should be reviewed against ADR-0004.
- **Single merge derivation.** `buildEffectiveBowtiePreview()` in `bowties.svelte.ts` is the only place the catalog×tree×metadata×layout merge happens. The previous fast/slow path branch and `EditableBowtiePreviewStore` class are gone; collapsing them was the structural fix for the "blank diagram during save" bug (ADR-0004 / spec 013 S2c).
- **Event ID identity is `EventIdKey`.** Every phase of the merge (catalog, layout, metadata, tree) routes incoming event-ID hex through `toEventIdKey()` from `$lib/utils/eventIdKey` before using it as a map key or set-membership check. `seenEventIds: Set<EventIdKey>` and `treeEntriesIndex: Map<EventIdKey, …>` carry the branded type end-to-end. This was the structural fix for the "same event appears twice in the preview" bug (ADR-0010 extension 2026-07-03) — never compare a raw `card.event_id_hex` or `TreeConfigValue.hex` string as identity; go through the branded type.
- **Uniqueness is enforced symmetrically at every phase.** The merge Owner promises "each `EventIdKey` appears at most once in `previews`", so every phase — including the catalog phase — gates on `seenEventIds.has(key)` before pushing. First phase to see a key wins; later phases skip. Adding a new merge phase means adding the same gate; the invariant is not delegated to upstream contributors (backend catalog, save-time re-emitters, Facility Bowtie Lifecycle composition, Config Draft Backend Mirror). Regression: `bowties.svelte.test.ts::dedupes duplicate catalog cards with the same event id (first wins)` — ADR-0010 addendum 2026-07-03.
- **Reclassify-into-bucket is symmetric with backend `merge_layout_metadata`.** When `card.ambiguous_entries` is drained into `producers`/`consumers` via a pending `bowtieMetadataStore.getRoleClassification(...)`, the emitted `EventSlotEntry` MUST have its `role` field rewritten to `'Producer'` / `'Consumer'` (mirror `bowties-core/src/bowtie/catalog.rs::merge_layout_metadata`'s `classified.role = ...` assignment). Downstream Consumers branch on `entry.role` (e.g., `BowtieCatalogPanel.confirmRemove`'s `isLastConsumer` check); leaving `role: 'Ambiguous'` silently sends removals down `doRemoveElement` instead of the delete-bowtie confirmation. The same loop also filters through `isEntryStillActive(entry, cardKey)` so stale ambiguous entries (leaf drifted away from the card key) are dropped, mirroring the filter already applied to `producers` / `consumers`. Regression: `bowties.svelte.test.ts::reclassified ambiguous entry has role rewritten to Consumer|Producer` and `BowtieCatalogPanel.test.ts::reaches the delete-bowtie confirmation when removing a reclassified last consumer` — 2026-07-03.
- **Pending deletions are facade-level.** The merge function does not consult `bowtieMetadataStore.hasPendingDeletion`; the facade applies that filter on top so the in-class merge remains pure with respect to deletion intent.

---

## Utils (`app/src/lib/utils/`)

| File | Purpose | Test |
|------|---------|------|
| `hexId.ts` | **Generic hex-ID helpers** — `formatCanonicalHex(bytes)`, `formatDottedHex(bytes)`, `parseHexId(input, expectedBytes)`. Single owner of the bytes ↔ hex-string rule (canonical contiguous uppercase, dotted uppercase, accepts `.`/`-`/space separators on parse). Both `serialize.ts` (8-byte EventID) and `nodeId.ts` (6-byte NodeID) delegate here. Mirror of `lcc_rs::types::{format_canonical_hex, format_dotted_hex, parse_hex_id}`. See ADR-0010. | `hexId.test.ts` |
| `eventIdKey.ts` | **`EventIdKey` — branded canonical event-ID identity (ADR-0010 extension 2026-07-03).** Compile-time guard against mixing dotted-display and canonical forms of an event ID as a map key or comparison. `toEventIdKey(hex)` normalizes dotted or canonical input to canonical; `eventIdKeyFromBytes(bytes)` produces canonical from `[u8;8]`; `formatEventIdKey(key)` renders dotted for display; `isCanonicalEventIdKey(str)` narrows. Consumed by `bowties.svelte.ts::buildEffectiveBowtiePreview` / `buildTreeEntriesIndex` and (progressively) by `bowtieMetadataStore`, `bowtieFocusStore`, `BowtieCard.svelte` display sites, and layout-YAML load boundaries. Single-variant today (real 8-byte events); designed to widen to a `PlaceholderSlot` sum type when placeholder-proxy wire-up lands. | `eventIdKey.test.ts` |
| `nodeId.ts` | Node ID normalization wrappers over `hexId.ts`: dotted-hex ↔ canonical. `nodeIdToDisplayHex(input)` converts any format to dotted-hex for UI display. `nodeIdStringToBytes` parses 6 bytes via `parseHexId(_, 6)`. | — |
| `nodeKey.ts` | **NodeKey** (Spec 014, ADR-0008, ADR-0010) — branded discriminated union covering live `NodeID` and `placeholder:<uuidv4>`. Exports `type NodeKey = LiveNodeKey | PlaceholderNodeKey`, constructor `nodeKey(input)`, `nodeKeyEquals`, `nodeKeyToString`, `nodeKeyToDisplay`, `toCanonicalNodeKey(input: string | NodeKey | null \| undefined)`, `isPlaceholderInput(input)`, and the transitional shim `NodeKeyInput = string \| NodeKey`. Mirrors the backend prefix predicate `layout::types::is_placeholder` exactly. Consumed by `configChanges.svelte.ts`, `editKey.ts`, and any store that legitimately widens to placeholders. | `nodeKey.test.ts` |
| `nodeRoster.ts` | S8: pure helpers comparing the active layout's saved-node roster against the currently-visible node set — `canonicalizeNodeId`, `computeDiscoveredOnlyNodeIds` (badge predicate, no threshold), `computeUnsavedInMemoryNodeIds` (threshold-gated by full capture — feeds `layoutStore.isDirty` and `addNode` save deltas), `isUnsavedDiscoveredNode`, `isSavedOffBusNode`. Consumed by `+page.svelte` (sidebar badge + dirty signal + save deltas), `configSidebarPresenter` (unsaved-new badge), and `nodeRoster.svelte.ts` (`injectOffBusSavedNodes`). | `nodeRoster.test.ts` |
| `nodeDisplayName.ts` | Display name resolution. `resolveNodeDisplayName` = SNIP fallback chain; `resolveNodePartsFromSnip` = structured SNIP-only resolver returning `NodeDisplayParts`; `resolveEffectiveUserName(tree, resolveValue)` = edit-layer tier (ACDI User Name leaf, space 251, draft→offline→baseline). Node-name surfaces compose `resolveEffectiveUserName(...) ?? resolveNodeDisplayName(...)`. Structured surfaces use `resolveNodeParts` from `$lib/layout`. | `nodeDisplayName.test.ts` |
| `formatters.ts` | Config value display formatting (int/string/eventId/float). `displayEventIdHex()` converts canonical contiguous hex to dotted display form. `isWellKnownEvent()` takes canonical hex. Event-id byte formatting goes through `formatEventIdHex` (re-exported from `serialize.ts` → `hexId.ts`); no local copy. | `formatters.test.ts` |
| `serialize.ts` | Serialize TreeConfigValue to raw bytes for writes. Event-ID helpers (`parseEventIdHex`, `canonicalEventIdHex`, `formatEventIdHex`, `normalizeEventIdHex`) are thin wrappers over `hexId.ts` — the byte-count rule lives there. | `serialize.test.ts` |
| `eventIds.ts` | Event ID placeholder detection; fresh event ID generation | — |
| `uuid.ts` | RFC 4122 v4 UUID generation via `crypto.getRandomValues` (broader Tauri-target compatibility than `crypto.randomUUID`). Single helper `generateUuidV4()`. No longer used by `placeholderBoardOrchestrator` (UUID minting moved to backend factory in S8.10); available for any future v4 UUID need (saved-connection ids, etc.). | — |
| `editKey.ts` | Canonical edit key construction: `nodeId:space:address` | `editKey.test.ts` |
| `displayResolution.ts` | **INTERNAL** (ADR-0004) — leaf-level resolution primitives consumed only by `$lib/layout` and the closely-related structural helpers. `resolveValue` waterfalls draft → offlinePending → baseline; `resolveRole` waterfalls pending edit → saved layout → catalog → CDI baseline; `makeValueResolver(nodeId)` is re-exported from `$lib/layout` for `buildElementLabel`/`getInstanceDisplayName`. | `displayResolution.test.ts` |
| `connectorConstraints.ts` | Evaluate connector slot constraints. **Spec 018 / S3:** reads only the pre-baked `slot.supportedDaughterboardConstraints[].validityRules` projection — the source-of-truth move from daughterboard `validityRules` to top-level `styles[].constraints` happens inside `bowties-core/src/profile/mod.rs::collect_validity_rules`. This file is unchanged by the migration. | `connectorConstraints.test.ts` |
| `connectorLeafDecision.ts` | Leaf value compatibility under slot constraints | `connectorLeafDecision.test.ts` |
| `connectorSlotSelectors.ts` | View model for connector slot selector UI | `connectorSlotSelectors.test.ts` |
| `cardTitle.ts` | Card title from CDI group name + user names | `cardTitle.test.ts` |
| `channelDefaults.ts` | Default channel name generation: `generateDefaultChannelName(nodeName, slotLabel, inputOrdinal)`. Pure function, no side effects. Spec 015 / S3. | `channelDefaults.test.ts` |
| `channelStyles.ts` | **Spec 018 / S2 (ADR-0013):** frontend style registry. Maps style id → event-leaf mapping. Exports `getStyleEventMapping(styleId)` returning `undefined` for unknown styles. Single source of truth for the lookup the `eventStateOrchestrator` and `+page.svelte` perform per channel; replaces the retired hardcoded `BOD_EVENT_MAPPING` constant. S2 ships `bod-block-detector-input` (`{occupied,clear} → {producerLeafIndex}`); **S5** adds `single-led-direct-lamp` (`{lit,unlit} → {consumerLeafIndex}`). `EventMappingEntry` carries `producerLeafIndex?` AND `consumerLeafIndex?` as mutually-exclusive optional fields (one per state, picked by the style's role). | `channelStyles.test.ts` |
| `channelState.ts` | **Spec 016 / S1:** Pure `deriveChannelState(events, occupiedId, clearId)` → `'no-config' \| 'unknown' \| 'occupied' \| 'clear'`. Compares timestamps from the event state store to determine live channel occupancy. **Spec 017 / S2:** Added `'no-config'` for channels whose event IDs cannot be resolved (placeholder, off-bus node, partial-capture saved tree) — distinct from `'unknown'` (resolved but no event seen). **Spec 018 / S5 (D3):** `OccupancyState` retired; the return type is now a discriminated union `ChannelState = { kind: 'no-config' } \| { kind: 'unknown' } \| { role: 'block-occupancy'; state: 'occupied' \| 'clear' } \| { role: 'lamp-indicator'; state: 'lit' \| 'unlit' }` so `{ role: 'lamp-indicator', state: 'occupied' }` is structurally unrepresentable. `deriveChannelState` takes a fourth `role` arg that dispatches the literal. Helpers `channelStateLabel(s)`, `channelStateClass(s)`, and `roleForChannelState(role)` keep components from re-pattern-matching the union. | `channelState.test.ts` |
| `treeLeafViewState.ts` | Display state for tree leaf rows (offline, compatibility) | `treeLeafViewState.test.ts` |
| `treeConfigValuePersistence.ts` | TreeConfigValue ↔ offline-stored string format | — |
| `layoutPath.ts` | Layout file path utilities: `normalizeLayoutTitle` strips extensions to get display name | `layoutPath.test.ts` |
| `menuEnableState.ts` | **Single owner of the native-menu enable policy** (S4). Pure `computeMenuEnableState(inputs: MenuEnableInputs) → MenuEnableState` derives the enable bits for the File/Connection menu items (View/Re-download CDI, Open/Close/Save/Save As, Sync to Bus, Add/Delete Placeholder Board). The route's reactive `$effect` builds the snapshot from its stores and feeds the result to the `update_menu_state` IPC. ADR-0011: reads the aggregate `hasInMemoryEdits` facade for offline Save, struct-only `layoutDirty` for the loaded-layout Save gate. | `menuEnableState.test.ts` |
| `xmlFormatter.ts` | Pretty-print XML for CDI viewer | — |

---

## API Layer (`app/src/lib/api/`)

| File | Purpose |
|------|---------|
| `tauri.ts` | Low-level Tauri IPC bindings (discovery, SNIP, PIP, registration) |
| `cdi.ts` | CDI download/cache operations |
| `config.ts` | Config read/write IPC (setModifiedValue, writeModifiedValues, discard) |
| `bowties.ts` | Bowtie catalog building and layout persistence |
| `layout.ts` | Layout file open/close, offline snapshot hydration. **Spec 018 / S6 bugfix (2026-07-03):** adds `syncLayoutDrafts(deltas)` and `clearLayoutDrafts()` TS wrappers over the new `layout_drafts.rs` IPCs — the frontend calls them to mirror facility/channel drafts into `LayoutState.drafts` before compose IPCs, and to drop the backend drafts on Discard / after Save. |
| `sync.ts` | Sync session and offline change reconciliation IPC |
| `connectorProfiles.ts` | Connector profile queries, slot selection, compatibility preview |
| `channels.ts` | Information channel inventory IPC (Spec 015): `listChannels`. Backend persists to `channels.yaml`; called by `channelsStore.loadChannels()`. **Spec 018 / S2 (ADR-0013):** `InformationChannel` now carries `{role, style, ownership, binding}` (discriminated union: `connectorInput` today, `lampRow` in S5) instead of `{channelType, hardwareRef}`; also exports `ChannelRole`, `ChannelOwnership`, `ChannelBinding` type aliases. **Atomic-save fold (2026-07-03, ADR-0002 extension):** `createChannels` / `renameChannel` / `deleteChannels` TS wrappers + backend commands were removed. Channel writes travel `save_layout_directory` as `createChannel` / `renameChannel` / `deleteChannel` `LayoutEditDelta`s (see `collectSaveDeltas.ts`). |
| `facilityBowties.ts` | **Spec 018 / S6 (new, D2):** thin TS wrapper `composeFacilityBowties(facilityId): Promise<CompositionOp[]>` around the `compose_facility_bowties` Tauri command. Mirrors the Rust `CompositionOp` shape (camelCase; `eventIdBytes: number[]`; `consumerLeafPath: string[]`). Consumed by `facilityOrchestrator.composeBowtiesIfWired` (compose path) and `tearDownFacilityBowties` (re-derivation to locate consumer leaves per T13 option (ii)). **Spec 018 / S6 bugfix (2026-07-03):** the orchestrator now calls `syncLayoutDrafts([...facilitiesStore.collectDeltas(), ...channelsStore.collectDeltas()])` from `$lib/api/layout` *before* every `composeFacilityBowties` invocation (both compose and teardown paths). The backend command reads through `LayoutState.effective_facilities()` / `effective_channels()` so freshly-added draft facilities/channels are visible. See ADR-0015 §"2026-07-03 extension". |
| `types.ts` | Shared API type definitions |

---

## Backend Commands (`app/src-tauri/src/commands/`)

| Module | Key Commands | Purpose |
|--------|-------------|---------|
| `connection.rs` | `list_serial_ports`, `get_layout_connections`, `save_layout_connections` | Per-layout connection registry — stored inside each layout manifest (Spec 013 / S4, S7). The global `$APPDATA/bowties/connections.json` registry was removed in S7; connections now live with the layout they belong to. |
| `startup.rs` | `get_known_layouts`, `add_known_layout`, `remove_known_layout` | Known-layout registry (`$APPDATA/bowties/known-layouts.json`) for the layout picker |
| `discovery.rs` | `discover_nodes`, `probe_nodes`, `register_node`, `query_snip_*`, `query_pip_*`, `verify_node_status`, `refresh_all_nodes` | Node discovery and metadata |
| `bowties.rs` | `query_event_roles`, `build_bowtie_catalog_command`, `get_bowties`, `set_bowtie_metadata`, `load_layout`, `save_layout`, `*_recent_layout`. Re-exports pure catalog functions from `bowties_core::bowtie::catalog`. **Slice 3a (2026-06-28):** the offline branch of `build_bowtie_catalog_command` (taken when `node_registry` has no live snapshots) now derives every projection — per-node config values, profile group roles, synthetic `DiscoveredNode` list for slot walking — directly from `state.layout_state` (`LayoutState.saved[].tree` + `cdi_xml`). The former `AppState.offline_bowtie_data` parallel cache was deleted. | Bowtie catalog and layout files |
| `cdi.rs` | `download_cdi`, `get_cdi_xml`, `get_cdi_structure`, `read_config_value`, `read_all_config_values`, `write_config_value`, `set_modified_value`, `write_modified_values`, `discard_modified_values`, `trigger_action`, `cancel_*` | CDI download, config read/write. **Slice 3a (2026-06-28):** former `proxy.set_cdi_data` / `set_cdi_parsed` write sites in `download_cdi`, `get_cdi_xml` file-cache hit, and `get_cdi_from_cache` are gone — fresh CDI bytes land via `LayoutState::record_captured`, and parsed CDI is re-parsed on demand. `get_cdi_xml` consults `LayoutState.cdi_xml(key)` before any disk-based fallback. The `proxy.get_cdi_data()` short-circuit at the top of `get_cdi_xml` is retained as a `SynthesizedNodeProxy` fast path (placeholders carry their bundled CDI in the proxy struct). **S4 (2026-07-18, ADR-0018 §2026-07-18 extension):** all config read/write reaches the peer via `AppState::peer_session(node_id)` (single DRY resolver) → `PeerSessionHandle::read_memory` / `write_memory` — `read_config_value`, `read_all_config_values` (batch planning + `fill_short_reply` continuation stay app-side; each batch/continuation read dispatched via the session), `read_memory_with_retry` (now takes `&PeerSessionHandle`), `write_config_value` + 2 more write sites, `trigger_action`. Closes the dual-path CDI-vs-config interleave regression (D1=A + D2=ii). **Fix 2 (cache-write guard):** `is_usable_cdi(&str)` (`MIN_USABLE_CDI_BYTES = 64`, non-empty, contains `</cdi>`) gates `write_cdi_to_cache` (refuses to overwrite a good cache with an invalid payload) and `download_cdi` surfaces a short/partial read as `RetrievalFailed` before `record_captured`/cache write. |
| `layout_capture.rs` | `capture_layout_snapshot`, `save_layout_directory`, `open_layout_directory`, `close_layout`, `create_new_layout_capture`, `build_offline_node_tree`, `save_layout_with_bus_writes` | Layout snapshot persistence. Thin command layer: fetches proxy data via `proxy_snapshot_data()` → delegates to `bowties_core::layout::capture::build_node_snapshot()`. Save commands accept `LayoutEditDelta[]` and return persisted `LayoutFile` plus (S8) `persistedNodeIds`. **2026-07-04:** The catalog side-channel merge (`merge_catalog_bowties_into`) was eliminated — bowtie metadata is now exclusively delta-backed; protocol-discovered role classifications flow through `LayoutState::discovered_roles()` (recorded at catalog rebuild time, read at save time, cleared after save). **Spec 017 / S3:** `open_layout_directory` calls `apply_profile_metadata_to_tree` (from `cdi.rs`) on each saved tree before `node_registry.set_saved_trees`, so trees seeded into live proxies on subsequent discovery carry `event_role` annotations — `bowties_core::channel_events::resolve_channel_event_ids` returns event IDs without requiring a CDI read. **Slice 3a (2026-06-28, ADR-0015):** the parallel-population of `state.offline_bowtie_data` (deleted in this slice) is gone from both `open_layout_directory` and `build_offline_node_tree`; the seeding loop now feeds only `cdi_xml_by_key` + `saved_trees_for_state`, which both flow into `LayoutState::from_loaded` and `node_registry::set_saved_trees`. `close_layout` no longer clears `offline_bowtie_data`. **ADR-0002 extension (2026-07-03):** `OpenLayoutResult.load_warnings: Vec<String>` propagates load-time referential-integrity repairs from `read_capture` → `LayoutDirectoryReadData.load_warnings` → the frontend `surfaceLoadWarnings` helper for user-visible surfacing. |
| `sync_panel.rs` | `set_offline_change`, `revert_offline_change`, `list_offline_changes`, `compute_layout_match_status`, `build_sync_session`, `apply_sync_changes` | Offline sync panel commands. Thin coordinator: delegates pure scoring to `bowties_core::sync::classifier::compute_layout_match`, CDI field resolution to `bowties_core::sync::field_meta`, change helpers to `bowties_core::sync::changes`. Owns only AppState reads/writes, bus I/O, and CDI path resolution (Tauri-dependent). **S4 (2026-07-18):** the targeted field read (~485) and write (~958) dispatch through `AppState::peer_session(node_id)` → `PeerSessionHandle::read_memory` / `write_memory` (D2=ii full closure — never the bare connection); `send_update_complete` stays on the bare connection (not a read/write primitive). |
| `connector_profiles.rs` | `get_connector_profile`, `preview_connector_compatibility` | Connector profile and slot constraints. Selection persistence (`get_connector_selections` / `put_connector_selections`) was removed in Spec 014; the replacement seam is `node_mode_selections` written through `placeholders.rs`. |
| `placeholders.rs` | `add_placeholder_board`, `list_bundled_profiles_command` | Spec 014 / S8.10: `add_placeholder_board` calls the placeholder factory (`placeholder.rs::synthesize`), inserts the `Synthesized` variant into the registry, and returns `{ nodeKey }`. `list_bundled_profiles_command` returns board-model summaries for the placeholder picker. Note: `set_node_mode_selection` and `clear_node_mode_selection` were removed per ADR-0012 — connector selections are now in-memory drafts collected as deltas at save time. |
| `channels.rs` | `list_channels` | Spec 015 / S1: reads `channels.yaml` from active layout via `bowties_core::layout::read_channels()` (ADR-0005 intent-shaped API). Returns empty vec when no layout is open or file missing (pre-015 backward compat). **Atomic-save fold (2026-07-03, ADR-0002 extension):** the previous write commands `create_channels` / `rename_channel` / `delete_channels` were removed. All channel mutations now travel as `LayoutEditDelta::{CreateChannel, RenameChannel, DeleteChannel}` through `save_layout_directory` (`layout::channels::apply_channel_deltas` sits beside `apply_facility_deltas`). |
| `facility_bowties.rs` | `compose_facility_bowties` | **Spec 018 / S6 (D2)**: thin IPC over `bowties_core::facility_bowties::compose_bowtie_ops`. **Spec 018 / S6 bugfix (2026-07-03):** reads facility + channels through `LayoutState.effective_facilities()` / `effective_channels()` (drafts-over-saved view). The frontend orchestrator calls `sync_layout_drafts` before this IPC so pending edits are visible; without that mirror the compose returned `unknown facility` for freshly-added draft facilities. Still consults `LayoutState` for behavior-template registry, per-node CDI trees, per-channel producer event-id resolution. Registered in `lib.rs::invoke_handler` + Tauri capabilities. Frontend wrapper is `app/src/lib/api/facilityBowties.ts::composeFacilityBowties`; frontend caller is `facilityOrchestrator.composeBowtiesIfWired` + `tearDownFacilityBowties` (same IPC used for teardown-time leaf resolution per T13 option (ii)). |
| `layout_drafts.rs` | `sync_layout_drafts`, `clear_layout_drafts` | **Spec 018 / S6 bugfix (new, 2026-07-03)** — mirrors the frontend's collected `LayoutEditDelta[]` into `LayoutState.drafts` so backend read paths (facility bowtie composition today; future consumers welcome) can observe frontend-side pending edits via `LayoutState.effective_*` accessors. `sync_layout_drafts` is idempotent w.r.t. any delta set — callers ship the *complete* current set on every sync. `clear_layout_drafts` is called on frontend Discard and inline at the end of `save_layout_directory` after the write completes. See ADR-0015 §"2026-07-03 extension: draft-layer activation". |
| `diagnostics.rs` | `get_diagnostic_report` | Ring-buffer logs, frame activity ring buffer, structured errors, and troubleshooting report with summary |

---

## Backend Domain Modules

### bowties-core crate (`bowties-core/src/`)

Pure domain logic extracted from `src-tauri` so that tests can run with
`cargo test` (no Tauri DLL dependency). Re-exported by `src-tauri` via
thin shim modules so existing `crate::` paths compile unchanged.

| Module | Purpose | Test |
|--------|---------|------|
| `node_key.rs` | **`NodeKey`** (ADR-0010) — sum type `Live(NodeID) \| Placeholder(Uuid)` with canonical wire form. | inline `#[cfg(test)]` (10) |
| `node_tree.rs` | Unified config tree: CDI + addresses + values + roles. Also owns `NodeRoles` (per-event producer/consumer sets). **Event ID canonical form:** `parse_event_id_hex()`, `normalize_event_id_hex()`, `bytes_to_display_hex()` are thin delegations to `lcc_rs::EventID` (`from_hex_string`, `to_canonical`, `to_hex_string`). The byte ↔ hex string rule lives in `lcc_rs::types`; nothing in `bowties-core` reimplements it. See ADR-0010. **Spec 018 quickchange (2026-06-30):** owns `replication_instances(parent, name) -> Vec<&GroupNode>` — the single sanctioned way to enumerate instances of a CDI replicated group (handles both wrapper and sibling shapes; see the **Replication Instance Traversal** seam). TS counterpart `replicationInstances` lives in `app/src/lib/types/nodeTree.ts`. | inline `#[cfg(test)]` (28+) |
| `node_proxy.rs` | Polymorphic node handle (`NodeProxyHandle` enum: `Live` + `Synthesized`). `LiveNodeProxy` actor; `SynthesizedNodeProxy` passive state holder. **Slice 3a (2026-06-28, ADR-0015 + ADR-0009 amendment):** `LiveNodeProxy` no longer holds CDI bytes — the `cdi_data` / `cdi_parsed` fields and their `ProxyMessage` variants are gone. **2026-07-04:** `config_values` field deleted from both `LiveNodeProxy` and `SynthesizedNodeProxy` — the catalog builder now reads from the authoritative config tree via `collect_event_id_leaves()`. All `GetConfigValues` / `SetConfigValues` / `MergeConfigValues` message variants and methods removed. **2026-07-04 (ADR-0015 Option C adopted):** `config_tree` field deleted from `LiveNodeProxy`. The tree lives in `LayoutState` (captured-over-saved precedence). `GetConfigTree` handler returns `None` for live proxies; `SetConfigTree` / `UpdateConfigTree` are no-ops. `SynthesizedNodeProxy` retains its `config_tree` (placeholders have no LayoutState entry until promoted). **Retained working buffers on `LiveNodeProxy`**: `snip`, `pip_flags` — per-actor bus-IO state; SNIP/PIP query dedup previously coalesced on the proxy via `snip_waiters` / `pip_waiters`. **S2 (2026-07-07, ADR-0016):** coalescing moved into `PeerSession`. `LiveNodeProxy` now holds `sessions: Option<Arc<PeerSessionRegistry>>` (D2 outcome — fetch-per-call, never a cached `PeerSessionHandle`). Two spawn constructors: `spawn` (no registry, in-actor fallback path retained for tests + pre-S2 wiring) and `spawn_with_sessions` (session-delegation path). `handle_query_snip` / `handle_query_pip` prefer the session when `sessions.get(node_id)` returns `Some`, falling back to the free-function shim (`lcc_rs::query_snip` / `pip::query_pip`) when the session hasn't been observed yet. Retained app-layer responsibilities audit (F4, 2026-07-07): `last_seen` / `last_verified` / `connection_status` observation, `Live \| Synthesized` polymorphism (ADR-0009), `DiscoveredNode` snapshot facade, externally-driven SNIP/PIP cache updates from `EventRouter`. | inline `#[cfg(test)]` |
| `node_registry.rs` | Thread-safe `NodeKey → NodeProxyHandle` map. **2026-07-04:** `saved_trees` seeding cache **deleted** — its job (seeding `LiveNodeProxy::config_tree` at spawn) is obsolete now that trees live in `LayoutState`. The same loop in `open_layout_directory` that populated `saved_trees` already feeds `LayoutState` via `trees_by_key`. **S2 (2026-07-07, ADR-0016):** owns `peer_sessions: RwLock<Option<Arc<PeerSessionRegistry>>>` injected by `set_peer_sessions()`. `get_or_create` reads the registry snapshot and passes it into `LiveNodeProxy::spawn_with_sessions`. Cleared on `shutdown_all`. | inline `#[cfg(test)]` |
| `layout/mod.rs` | **Deep module** (ADR-0005). Sole owner of layout directory file structure. Public API: `save_capture`, `read_capture`, `read_node_snapshot`, `update_offline_changes`, `update_channels`, `read_channels`, etc. Layout identity = folder path. `read_capture` guarantees a referentially-consistent view (`facilities.yaml` slot bindings only cite channel ids present in `channels.yaml`); repairs are surfaced through `LayoutDirectoryReadData.load_warnings` and applied in-memory only — the fix reaches disk on the next save (ADR-0002 extension 2026-07-03). | inline `#[cfg(test)]` (10 tests) |
| `layout/types.rs` | YAML data structures, `ConnectionConfig`, `LayoutEditDelta` (incl. `AttachChannelToSlot` / `DetachChannelFromSlot` per Spec 018 / S4 plus `CreateChannel` / `RenameChannel` / `DeleteChannel` per the 2026-07-03 atomic-save fold — see ADR-0002 extension), `apply_layout_deltas` (no-ops on facility and channel deltas; the work is in `layout/facilities.rs::apply_facility_deltas` and `layout/channels.rs::apply_channel_deltas`). | inline `#[cfg(test)]` |
| `layout/io.rs` | Layout directory read/write, node-file path derivation, CDI XML resolution. Constants: `MANIFEST_FILE`, `BOWTIES_FILE`, `CHANNELS_FILE`, etc. `LayoutDirectoryWriteData` / `LayoutDirectoryReadData` include all companion files. `LayoutDirectoryReadData` also carries `load_warnings: Vec<String>` (ADR-0002 extension 2026-07-03) — populated by `read_layout_capture` from `facilities::normalize_facility_channel_refs` so callers can surface load-time repairs to the user. | inline `#[cfg(test)]` |
| `layout/journal.rs` | **Write-ahead journal** (ADR-0006). In-place writes guarded by marker + backup. | inline `#[cfg(test)]` (8) |
| `layout/manifest.rs` | Layout manifest (schema v4, no `companion_dir`), saved connections. | inline `#[cfg(test)]` |
| `layout/known_layouts.rs` | App-level known-layout registry (Spec 013 / S5). | inline `#[cfg(test)]` (7) |
| `layout/node_snapshot.rs` | Node config snapshot for offline use. | inline `#[cfg(test)]` |
| `layout/offline_changes.rs` | Offline change staging (config diffs). | inline `#[cfg(test)]` |
| `layout/channels.rs` | Information channel domain types (`InformationChannel`, `ChannelRole`, `ChannelOwnership`, `ChannelBinding`, `ChannelsDocument`). Owns `channels.yaml` schema, serde, and round-trip. Persistence via `mod.rs` intent-shaped API (`read_channels`, `update_channels`). Spec 015; **Spec 018 / S2 (ADR-0013)** retired the legacy `ChannelType` enum + `HardwareReference` struct in the same change set and bumped `SCHEMA_VERSION` 1.0 → 2.0 (no migration; pre-018 layouts fail to load per FR-009). Binding is a `#[serde(tag = "kind")]` discriminated union; both `connectorInput` and `lampRow` variants are declared now (S5 constructs `lampRow`). **Atomic-save fold (2026-07-03, ADR-0002 extension):** `apply_channel_deltas(doc, deltas) -> Result<(), ChannelApplyError>` handles the full `CreateChannel` / `RenameChannel` / `DeleteChannel` triplet (was `CreateUserOwnedChannel`-only under S5). Called from `layout_capture.rs::save_layout_directory` beside `apply_facility_deltas`; errors: `DuplicateChannelId`, `UnknownRenameTarget`; delete is idempotent for unknown ids. | inline `#[cfg(test)]` (16 tests) |
| `layout/facilities.rs` | **Facility domain types** (Spec 018 / S1 → S4) — `Facility` (UUID id + `template_id` + `name` + **`slot_bindings: BTreeMap<String, Vec<String>>`** per D8) and `FacilitiesDocument` (schemaVersion 1.0). Empty `Vec` means the slot is unbound; multi-element bindings are valid in the wire form (forward-compat for ABS aspect-slot repeaters) and bounded per slot by the template's `max_channels`. Status (`Incomplete`/`Wired`) is derived in `effectiveLayoutStore`, never persisted on the entity. Persistence via `mod.rs` intent-shaped API (`read_facilities`, `update_facilities`); file `facilities.yaml` is optional (pre-018 layouts read as empty). `LayoutDirectoryReadData` / `WriteData` carry it alongside channels. **S4 additions:** `pub fn apply_facility_deltas(doc, deltas) -> Result<(), FacilityApplyError>` enforces per-slot `max_channels` via `behavior_templates::find_template(...).find_slot(...)`; idempotent on re-attach + on detach of an absent channel; returns `UnknownFacility` / `UnknownSlot` / `SlotAtMax` / `UnknownTemplate` errors otherwise. Called from `app/src-tauri/src/commands/layout_capture.rs::save_layout_directory` immediately before `apply_layout_deltas` against the same delta list (D7). **Referential-integrity normalize (2026-07-03, ADR-0002 extension):** `pub fn normalize_facility_channel_refs(facilities, channels) -> Vec<String>` drops slot-binding channel ids that don't resolve in `channels.channels` and returns one human-readable warning per removal. Called from `read_layout_capture` so every backend Consumer sees a schema-clean view — closes the seam-symmetry gap between `compose_bowtie_ops` (hard-errored on dangling refs), the cardinality guard (counted them toward maxChannels), and the "Used by" render path (silently filtered). | inline `#[cfg(test)]` (19 tests) |
| `layout/serde_node_id.rs` | Custom serde for NodeID (dotted-hex in YAML). | — |
| `profile/mod.rs` | Profile tree annotation, overlay composition, cache types. Owns the bundled profile fixture tests that now cover detector daughterboard producer-action constraints (`RR-CirKits.shared-daughterboards.yaml`). **Polarity invariant (2026-06-26):** the per-replication producer-action constraints on BOD detector boards encode *logical* input transitions, not raw voltage transitions. With `Input Function = Active Lo` (legacy) or `Indication polarity = Low (0V)` (rev-C7), the detector output going low (block occupied) drives the logical input ON. Therefore slot 1 (producerLeafIndex 0 = occupied) must be `Input On` and slot 2 (producerLeafIndex 1 = clear) must be `Input Off`. Reversing either the rule values or the `eventMapping` indices flips the channel-state display. **Test split (Spec 018 / S1.1):** capability tests that exercise `build_connector_profile` code paths (e.g. `build_connector_profile_handles_empty_affected_paths_slot`) load minimal hand-crafted fixtures from `bowties-core/tests/fixtures/structure-profiles/` and pass `library = None` whenever possible — they MUST NOT pin assertions on a shipping profile filename. Tests that legitimately validate a bundled profile's contract (Tower-LCC QUICKSTART, CdiSignature selector, captured-CDI parity) keep their bundled dependencies and are documented in the S1.1 audit table. Schema-conformance of shipping profiles is checked separately by `tests/bundled_profiles_smoke.rs` with no content assertions, so future profile renames or content edits don't ripple into algorithm tests. | inline `#[cfg(test)]`; `tests/bundled_profiles_smoke.rs` |
| `profile/types.rs` | Structure profile types (event roles, relevance, connectors). Includes `ChannelInputMapping` (Spec 015 / S3) — pin-to-template capability map per daughterboard, carried through to frontend `DaughterboardView.channelInputs`. **Spec 018 / S3 (ADR-0013):** `SharedDaughterboardLibrary` gains a `styles: Vec<Style>` catalog; `Style { style_id, binding_kind, constraints, constraint_variants }` and `StyleConstraintVariant { variant_id, replace_base_constraints, constraints }` own the **Style Constraint Contract** that used to live inline on BOD-* daughterboard `validity_rules` entries. | — |
| `profile/resolver.rs` | Profile conditional resolution (firmware checks). | inline `#[cfg(test)]` |
| `placeholder.rs` | **Placeholder factory helpers** — CDI loading, EventId-zero collection, config-value merging, leaf-default population. All pure (no Tauri deps). | inline `#[cfg(test)]` (5 tests) |
| `behavior_templates.rs` | **Behavior template registry** (Spec 018 / S1 → S4) — hardcoded `BehaviorTemplate` / `SlotDefinition` / `SlotKind` / `StateMapping` types; the `BLOCK_INDICATOR` constant + `registered_templates()` + `find_template()` accessors. Serialize-only (frontend has its own mirror type); no `Deserialize` because the registry is code-level, not loaded from YAML. Exposed to the frontend by the `list_behavior_templates` IPC. **S4 additions:** `SlotDefinition` gains `min_channels: u32, max_channels: Option<u32>` (D8 cardinality contract); `is_at_max(current_count)` helper; `BehaviorTemplate::find_slot(label)` lookup. Block Indicator declares both slots `min: 1, max: Some(1)` — future templates default to declaring `max_channels` explicitly (e.g. ABS aspect-slot repeaters will raise the cap). | inline `#[cfg(test)]` (8 tests) |
| `facility_bowties/mod.rs` | **Spec 018 / S6 (new, D2 + D6) — Owner of the Facility Bowtie Lifecycle composition seam (deepest layer).** Pure Rust module. `pub fn compose_bowtie_ops(facility, template, channels, producer_event_ids, per_node_cdi, consumer_leaf_index) -> Result<Vec<CompositionOp>, FacilityCompositionError>` computes the set of "write this event ID onto that consumer leaf + register a bowtie with this back-reference" pairs for a Wired facility. Each `CompositionOp` carries `consumer_node_key`, `consumer_leaf_path`, `consumer_leaf_space`, `consumer_leaf_address`, `event_id_bytes: [u8; 8]` (adopted verbatim from the producer per D6), `bowtie_name` (`"{facility.name} — {consumer_command}"`), `created_by_facility` back-reference. Errors: `NotWired { slot_label }` (guard: every slot must be at its `min_channels`), `MissingProducerEventId`, `MissingConsumerLeaf`, `UnknownChannel`, `RoleMismatch`, `NoProducerSlot`, `NoConsumerSlot`. **T13 architect resolution:** teardown does NOT persist a separate leaf back-reference; it re-invokes this composer on the still-Wired facility shape and reuses the returned ops' leaves to locate the consumer fields it must overwrite with fresh event IDs (option (ii) — trades one extra IPC round-trip for schema locality). **Spec 018 / S6 bugfix (2026-07-03) — asymmetric role match:** `collect` / `role_matches` here treat `event_role: None` and `Ambiguous` leaves as matching any expected role (mirrors frontend `_roleMatches` in `effectiveLayoutStore`). Safe because the composer's consumer resolution scopes to a **single-role subtree by profile design** (`Direct Lamp Control/Lamp #N` is Consumer-only). `channel_events::collect_from_children` stays **strict** — its scope is a mixed-role subtree (Tower-LCC connector line) where permissive matching would corrupt `producerLeafIndex` ordinals. Defence-in-depth against profile-annotation timing races on live proxy trees (see cdi.rs `build_node_config_tree` path in the config-read completion — trees can transiently carry `event_role: None` before the last-node profile pass runs). | inline `#[cfg(test)]` |
| `bowtie/mod.rs` | Bowtie catalog module root. | — |
| `bowtie/types.rs` | Bowtie catalog types: `BowtieState`, `EventSlotEntry`, `BowtieCard`, `BowtieCatalog`. | — |
| `bowtie/catalog.rs` | **Bowtie catalog builder** — pure algorithm: slot walking, catalog building, layout metadata merging, role extraction. Also owns `SlotInfo`, `WELL_KNOWN_EVENT_IDS`, `node_display_name`, `CdiReadCompletePayload`. | inline `#[cfg(test)]` (25+ tests) |
| `layout/capture.rs` | **Snapshot builder** — pure tree-walking and snapshot assembly logic. Extracts `collect_leaf_values`, `group_key`, `build_node_snapshot` from src-tauri. Takes `ProxySnapshotData` input struct. **ADR-0015 contract pin:** `cdi_xml_len: Some(N)` ⇒ fingerprint = `"len:N"`, never `"missing"` — the structural guarantee that makes the src-tauri `proxy_snapshot_data` LayoutState fallback eliminate the silent-drop save bug. | inline `#[cfg(test)]` (10 tests) |
| `layout/state.rs` | **`LayoutState` deep module** (ADR-0015). Single in-memory owner of the open layout's three-layer projection: `saved` (mirrors disk), `captured` (fresh-from-bus not yet persisted), `drafts` (frontend edits awaiting save), plus `discovered_roles` (protocol-discovered role classifications from catalog rebuilds). Public API: `from_loaded(root, LayoutDirectoryReadData, cdi_xml_by_key, trees_by_key)`, queries (`persisted_node_keys`, `saved_node`, `captured_node`, `cdi_xml`, `config_tree`, `snapshot_for_save`, `discovered_roles()`, plus **Spec 018 / S6 bugfix** `effective_facilities()` / `effective_channels()` — drafts-over-saved merged views), mutations (`record_captured`, `record_discovered_roles(roles)` / `clear_discovered_roles()`, `sync_drafts(deltas)` / `clear_drafts()` — replace the earlier `merge_drafts` placeholder; plus mut accessors for slice-2 expansion). **2026-07-04:** `discovered_roles: BTreeMap<String, RoleClassification>` added — catalog rebuild sites (`build_bowtie_catalog_command` in `bowties.rs`, CDI completion in `cdi.rs`) call `record_discovered_roles()` after extracting resolved classifications; save flow reads via `discovered_roles()` with `or_insert_with` (user-explicit classifications win); cleared after save. Eliminates the catalog side-channel merge that bypassed ADR-0002. **Captured-vs-saved precedence (slice-1 contract):** `cdi_xml(key)` and `config_tree(key)` always prefer captured over saved. **Drafts-over-saved precedence (Spec 018 / S6):** `effective_facilities()` / `effective_channels()` return `drafts.pending_*` when set, else `saved`; populated by `sync_drafts` via the same `apply_facility_deltas` / `apply_channel_deltas` the save flow uses (idempotent w.r.t. delta set). `DraftLayer` carries `pending_facilities: Option<FacilitiesDocument>` + `pending_channels: Option<ChannelsDocument>` (bowtie-metadata drafts stay frontend-only — YAGNI). `DraftSyncError` wraps facility + channel apply errors. |low source of truth); `LiveNodeProxy::cdi_data` / `cdi_parsed` and their `ProxyMessage` variants were removed. Original design rationale in [specs/archive/backend-layout-state.md](../specs/archive/backend-layout-state.md) (historical reference only). | inline `#[cfg(test)]` (11 tests) |
| `sync/mod.rs` | Sync domain logic module root. | — |
| `sync/changes.rs` | Offline change helpers: `same_change_target`, `remove_changes_by_id`. | inline `#[cfg(test)]` (5 tests) |
| `sync/field_meta.rs` | CDI field metadata resolution, value parsing/conversion, snapshot field label resolution. Owns `FieldMeta`, `find_field_meta_in_cdi`, `raw_bytes_to_value_string`, `string_to_config_value`, `field_meta_to_leaf`. | inline `#[cfg(test)]` (10 tests) |
| `sync/classifier.rs` | Layout match scoring (`compute_layout_match`), sync row classification (`classify_sync_row`), sync session/result types. | inline `#[cfg(test)]` (7 tests) |

### Tauri app shell (`app/src-tauri/src/`)

| Module | Purpose | Test |
|--------|---------|------|
| `lib.rs` | Entry point: connection init, state setup, command registration. Window close is handled by the frontend (see `+page.svelte` `onCloseRequested`); the backend provides `disconnect_lcc` as an IPC command but does not intercept the close event. | — |
| `main.rs` | Tauri desktop app launcher | — |
| `state.rs` | Authoritative app state: connection, registry, caches, `TuningConfig` (loaded from `tuning.toml` in app data dir). Owns `layout_state: Arc<RwLock<Option<bowties_core::layout::state::LayoutState>>>` as the single in-memory home for an open layout's persistent CDI / trees / values. Re-exports `NodeRoles` from `bowties_core::node_tree` and bowtie catalog types from `bowties_core::bowtie::types`. **Slice 3a (2026-06-28):** the `OfflineBowtieData` struct and the `offline_bowtie_data: Arc<RwLock<OfflineBowtieData>>` field were removed — the offline catalog rebuild now derives its projections from `layout_state` directly. **S2 (2026-07-07, ADR-0016):** owns `sessions: Arc<RwLock<Option<Arc<PeerSessionRegistry>>>>` constructed in `set_connection_with_dispatcher` alongside `NetworkSession` and published to `NodeRegistry::set_peer_sessions`. Cleared (via `sessions.clear().await`) in `disconnect`. **S3 (2026-07-08, ADR-0018):** `cdi_inflight: CdiInflightRegistry` and `cdi_download_cancel: Arc<AtomicBool>` fields **removed**. Per-peer FIFO CDI serialisation is now structural via `PeerSession::cdi_pending: VecDeque`, and cancellation flows through `PeerCommand::Cancel` dispatched to sessions from `commands::cdi::cancel_cdi_download`. | inline `#[cfg(test)]` |
| `node_key.rs` | Re-export shim → `bowties_core::node_key` | — |
| `node_registry.rs` | Re-export shim → `bowties_core::node_registry` | — |
| `node_proxy.rs` | Re-export shim → `bowties_core::node_proxy` | — |
| `node_tree.rs` | Re-export shim → `bowties_core::node_tree` | — |
| `layout/mod.rs` | Re-export shim → `bowties_core::layout` | — |
| `profile/mod.rs` | Re-export shim → `bowties_core::profile` + keeps `loader` submodule | — |
| `profile/loader.rs` | `.profile.yaml` loading; depends on `tauri::AppHandle` for resource-dir resolution. Bundled-profile listing for placeholder picker. | inline `#[cfg(test)]` |
| `placeholder.rs` | **Placeholder factory orchestrator** (S8.10). Owns `synthesize`/`reconstitute` (Tauri-dependent). Re-exports pure helpers from `bowties_core::placeholder`. | — |
| `diagnostics.rs` | Ring-buffer logging (`bwlog!`), diagnostic stats, frame activity ring buffer (`FrameRing`), structured errors (`DiagError`), human-readable summary | — |
| `events/router.rs` | Event broadcast: transport frames → Tauri events | inline `#[cfg(test)]` |
| `traffic/mod.rs` | Message decoding for traffic monitor display | — |
| `menu.rs` | Native OS menu bar: builds File / Edit / View / Tools submenus on all platforms (+ Help on Windows/Linux, + App submenu first on macOS), returns `MenuHandles` for runtime enable/disable. Edit uses `PredefinedMenuItem` cut/copy/paste/select_all everywhere; undo/redo are added only on macOS because WebView2/WebKitGTK don't respond to the platform undo/redo commands the predefined items dispatch, so on Windows/Linux they would appear as non-functional entries (Ctrl+Z / Ctrl+Y still work natively inside text fields there). The Edit menu is required on macOS so Cmd+C/V reach WKWebView text fields via the AppKit responder chain (issue #19); on Windows/Linux it's discoverability chrome since the WebView handles those shortcuts natively. On macOS the App submenu hosts About/Services/Hide/Quit; About and Quit are custom `MenuItem`s (ids `menu-about`, `menu-exit`) on all platforms so `on_menu_event` still fires the frontend AboutDialog / unsaved-changes exit prompt; Services/Hide/HideOthers/ShowAll must be predefined. | — |
---

## lcc-rs Protocol Library (`lcc-rs/src/`)

| Module | Purpose | Test |
|--------|---------|------|
| `lib.rs` | Public crate API; re-exports types and protocol structs | — |
| `types.rs` | Core types: NodeID, EventID, NodeAlias, SNIPData, ProtocolFlags | inline `#[cfg(test)]` |
| `constants.rs` | Protocol constants (timeouts, buffer sizes, pacing delays) | — |
| `datagram_reader.rs` | Unified datagram read exchange: sends request, handles ACK/reject/timeout-extension, assembles reply, ACKs, applies post-ACK pacing delay. Shared by CDI download and single reads. Owns `MemoryReadConfig`, `ExchangeResult`, `ReadDescriptor`. **`use_send_direct` invariant (2026-07-05):** callers running a *sequential* datagram-exchange loop against a single destination (BatchReader, CDI read) MUST pass `use_send_direct: true` so both the request and the reply-ACK bypass the writer-task mpsc wakeup. Otherwise unrelated queued traffic (SNIP/PIP writes for other nodes fired concurrently by discovery) can slip between the reply-ACK and the next request on slow serial transports, causing the target to reject the next request with `DatagramRejected 0x2020`. Single-shot reads (`read_memory_timed`) still use `false`. | inline `#[cfg(test)]` — 9 tests |
| `discovery.rs` | LccConnection: protocol orchestrator, node probe. `read_cdi_with_handle` uses `use_send_direct: true` (see `datagram_reader.rs` invariant). **S3 (2026-07-08, ADR-0018):** `read_cdi_cancellable_with_stats` is `#[deprecated(note = "route through PeerSessionHandle::download_cdi; retired in S5")]` — remains as a compatibility shim for external `LccConnection` consumers, has zero in-tree production callers. **S4 (2026-07-18, ADR-0018 §2026-07-18 extension):** the `BatchReader` / `BatchReadResult` / `batch_reader` / `batch_reader_with_config` machinery is **deleted** (no callers after config r/w moved onto `PeerSession`); `read_memory` / `read_memory_timed` / `write_memory` and the plain `BatchReadDescriptor` data struct are retained. | inline `#[cfg(test)]` |
| `alias_allocation.rs` | CID7→CID4 alias allocation, conflict detection per S-9.7.2.1. Uses standard LFSR generator (ported from OpenLCB_Java `NIDa`) for candidate aliases; `AliasGenerator` struct owns the 48-bit LFSR state. Retries up to `MAX_ALIAS_ATTEMPTS` (20) with pseudo-random candidates on conflict. | inline `#[cfg(test)]` — 5 tests |
| `snip.rs` | SNIP query via addressed message: manufacturer/model/version retrieval; tolerant of devices that emit SNIP payload as repeated `DatagramOnly` chunks (parse at silence boundary). Version byte (byte 0) is skipped without validation for JMRI-level interop — accepts 0x01, 0x04, or any value. User-section version byte likewise skipped. **S2 (2026-07-07):** `query_snip` is retained as a compatibility shim with unchanged public signature. New callers go through `PeerSessionHandle::query_snip()` (see `peer_session.rs`); the shim exists for `LiveNodeProxy`'s no-registry fallback path and residual internal callers pending S5 retirement. | inline `#[cfg(test)]` |
| `pip.rs` | Protocol Identification Protocol capability query. **S2 (2026-07-07):** shim status same as `snip.rs`. | inline `#[cfg(test)]` |
| `peer_session.rs` | Per-peer session actor (S2, ADR-0016; S3 CDI extension, ADR-0018). Owns `PeerSession`, `PeerCommand`, `PeerError`, `PeerSessionHandle`, and the internal `ActiveExchange` state machine. One task per remote NodeID; owns SNIP/PIP cache + coalescing waiters (`snip_waiters` / `pip_waiters: Vec<oneshot::Sender>`), cache lifecycle (cleared on `PeerCommand::PeerReinitialised`), in-place alias updates (`PeerCommand::AliasChanged`), and the mid-exchange `Wedged` abort (D1 outcome — via a per-session health-forwarder task that translates the Transport Health watch into `PeerCommand::TransportWedged`). **S7 (2026-07-18, ADR-0016 §2026-07-18 extension):** `self.inbound` is a per-peer `mpsc::Receiver<InboundEvent>` (fed by the registry demux, `INBOUND_CAPACITY = 256`) — no longer a shared `subscribe_all()` broadcast; the per-frame source-alias filter is dropped (dest check kept). `InboundEvent::Lagged(pending)` (the mpsc-overflow analog of the old broadcast `Lagged`) triggers `handle_inbound_lag`: for an active `CdiDownload` it does **bounded in-place recovery** (reset the chunk assembler + re-issue the same-`address_cursor` read via `send_next_chunk_request`, bounded by the cumulative-per-download `lag_recovery_count` = `config.max_retries`); on exhaustion it aborts through the corrected cleanup gate; for SNIP/PIP it retains the D3 abort-and-continue (caches preserved); when idle it is ignored. Peer-cleanup emission is now gated on `PeerError::is_our_fault_live_wire()` (single source of truth: `Cancelled`/`Rejected`/`Protocol`/`Timeout` emit; `TransportUnhealthy`/`PeerReinitialised`/`AliasChanged`/`NotSupported`/`NotConnected` do not) — replacing the `matches!(err, Cancelled | Rejected)` allowlist; exactly-once preserved because CDI `Timeout` emits via `handle_deadline`, not `abort_active`. Typed convenience methods (`query_snip`, `query_pip`, `download_cdi`, `read_memory`, `write_memory`, `cancel`) construct the appropriate `PeerCommand` + oneshot and await the reply. **S4 (2026-07-18, ADR-0018 §2026-07-18 extension):** `ActiveExchange::MemoryRead` / `MemoryWrite` are single-datagram config primitives (`PeerCommand::ReadMemory` / `WriteMemory`) — read = one memory-config read round-trip (arbitrary `AddressSpace::from_u8`, reply-identity guard, sole-ACK-owner, returns `(Vec<u8>, discovery::MemoryReadTiming)`); write = RequestWithNoReply (`DatagramReceivedOk` = success, resend-OK DR retry to `WRITE_MEMORY_MAX_RETRIES`, `>64`-byte payloads chunked by the handle method). Concurrent read/write ops serialise via a unified `mem_pending` FIFO (drained after `cdi_pending`); both variants are wired into every actor match arm (deadline, dispatch, lag, deadline-expiry, abort, drain). D1=A: CDI-element batch planning stays app-side in `commands/cdi.rs`; only the datagram exchange lives here.ADR-0018):** `ActiveExchange::CdiDownload` owns the CDI multi-chunk state machine (build/send `MemoryConfigCmd::build_read` datagram → reassemble reply → ACK → advance cursor → null-terminator termination); DR-with-resend-OK retry cap defended per-chunk (`chunk_retry_count`, `config.max_retries`); OIR-terminal decode wraps `PeerError::Rejected { mti: u32, code: u16 }` (mti widened from u16 in S3). Second concurrent `PeerCommand::DownloadCdi` on the same peer queues in `cdi_pending: VecDeque` (FIFO) — no `CdiInflightRegistry` in the codebase. Peer-cleanup contract: `emit_terminate_due_to_error(code)` fires exactly once for `Timeout`/`Cancelled`/`Rejected`; skipped for `Wedged`/`PeerReinitialised`/`AliasChanged` (wire dead or peer already released state). **2026-07-14 (ADR-0018 §2026-07-14 extension):** `on_cdi_frame` now owns reply-identity — destructures `address` from `ReadReply::Success` and returns early (ACK already sent, cursor NOT advanced) when it differs from `active.address_cursor`; on `assembler.handle_frame(...) → Err(_)` calls `assembler.clear_source(self.alias)` and returns rather than emitting `TerminateDueToError`. Both changes preserve peer-cleanup for genuine our-fault failures. | `tests/peer_session_snip_pip.rs` (14 integration tests) + `tests/peer_session_cdi.rs` (integration tests) + `tests/peer_session_config.rs` (7 integration tests — S4 read/write primitives, FIFO serialisation, CDI+read interleave regression, timeout/OIR/Wedged) |
| `peer_session_registry.rs` | Sole spawner of `PeerSession` actors (S2, ADR-0016). Owns `Arc<RwLock<HashMap<NodeID, PeerSessionHandle>>>` with the read-check → drop-guard → build-outside-lock → write-lock-double-check discipline (per `tokio-rwlock-self-deadlock` guardrail). Background spawn-watcher task subscribes to `TransportHandle::subscribe_all()` and qualifies inbound frames by MTI: `VerifiedNode` (0x19170), `InitializationComplete` (0x19100), `AliasMapDefinition` (0x10701). Repeat observations dispatch `PeerCommand::AliasChanged` — never a re-spawn. Frames that carry only an alias never trigger session creation. Public API: `new(transport, our_alias) -> Arc<Self>`, `get(node_id) -> Option<PeerSessionHandle>`, `remove(node_id)`, `clear()`, `shutdown()`. **S7 (2026-07-18, ADR-0016 §2026-07-18 extension):** owns per-peer inbound routing — `routes: RwLock<HashMap<u16, Route>>` keyed by **source alias** (`Route { tx: mpsc::Sender<InboundEvent>, pending_drops: Arc<AtomicU64> }`); a `demux_task` drains the retained `subscribe_all()` bus and forwards each addressed/datagram frame to the destination peer's bounded channel, coalescing an owed `InboundEvent::Lagged(pending)` before the next successful frame on channel-full, and signalling all routes on a broadcast-level lag. Spawn-watcher registers the route atomically with the session entry (lock order **sessions → routes**) and re-keys both route and `entry.alias` on `AliasChanged`; `remove`/`clear`/`shutdown` prune routes and abort the demux task. Construction-window contract = **DROP** (a frame for an unregistered source alias is discarded; safe because a session issues no requests before registration). **2026-07-14 (ADR-0016 §2026-07-14 extension):** spawn-watcher `JoinHandle` lives inside `Mutex<Option<JoinHandle<()>>>` (renamed from the detached `_spawn_watcher: JoinHandle` field that leaked past drop). `pub async fn shutdown(&self)` clears the sessions map, `.take()`s the handle, `abort()`s, and awaits the aborted result. Aggregate owns its spawn-watcher lifecycle; matches `TransportActor::shutdown` / `LccConnection::shutdown_responders`. `AppState::disconnect` calls `sessions.shutdown().await` so the Windows serial handle releases before reconnect. See `aiwiki/seams.md` **Peer Session Ownership** entry. | `tests/peer_session_snip_pip.rs` (14 integration tests) + `tests/peer_session_registry.rs` (1 shutdown regression test) |
| `transport_actor.rs` | Dual-path frame I/O actor: mpsc queue (`send`) + direct-write inline (`send_direct`). Both paths ultimately serialize on the same `Arc<Mutex<Box<dyn TransportWriter>>>`; the difference is scheduler-hop latency between adjacent frames (mpsc adds a writer-task wakeup, ~13 ms on Windows tokio). **`direct_write_count()` observability (2026-07-05):** `TransportHandle` exposes an `AtomicUsize` counter incremented on each `send_direct`; used by discovery tests to pin the CDI-loop `use_send_direct: true` invariant against future reverts. **Transport Health seam (2026-07-07, S1 of spec 019):** owns `TransportHealth { Healthy \| Degraded \| Wedged }`, `SERIAL_SEND_TIMEOUT = 500ms`, `TCP_SEND_TIMEOUT = 2000ms`, and the `send_frame_with_timeout(writer, frame, health_tx)` helper. Both `writer_loop` and `send_direct` publish through the helper; `TransportHandle::send()` short-circuits with `Error::TransportUnhealthy` while wedged; consumers subscribe via `TransportHandle::subscribe_health() -> Option<watch::Receiver<TransportHealth>>`. See `aiwiki/seams.md` **Transport Health** entry. | inline `#[cfg(test)]` |
| `dispatcher.rs` | Inbound frame routing by MTI & alias | inline `#[cfg(test)]` |
| `cdi/mod.rs` | Cdi struct and DataElement enum | — |
| `cdi/parser.rs` | CDI XML → Cdi tree parsing | inline `#[cfg(test)]` |
| `cdi/hierarchy.rs` | Tree traversal: `walk_event_slots()` iterator | inline `#[cfg(test)]` |
| `cdi/role.rs` | Event role classification heuristics | inline `#[cfg(test)]` |
| `protocol/mod.rs` | Protocol facade; re-exports | — |
| `protocol/frame.rs` | GridConnectFrame: CAN header parse/encode; `parse_wire()` variant skips 29-bit validation for adapters with non-standard header encoding | inline `#[cfg(test)]` |
| `protocol/mti.rs` | MTI enum: 60+ message types with encoding | inline `#[cfg(test)]` |
| `protocol/datagram.rs` | DatagramAssembler: multi-frame reassembly | inline `#[cfg(test)]` |
| `protocol/memory_config.rs` | MemoryConfigCmd, address spaces, ReadReply parsing | inline `#[cfg(test)]` |
| `transport/mod.rs` | Transport trait API | — |
| `transport/tcp.rs` | TCP transport with GridConnect framing | inline `#[cfg(test)]` |
| `transport/gridconnect_serial.rs` | Serial transport with GridConnect framing; owns `FrameEncoding` enum (Standard / MergCanRs) for adapter-specific header codec. **Blocking OS reader/writer threads** bridged to tokio via mpsc — kept for **executor isolation** (a stuck serial syscall never starves a tokio worker; not the earlier unproven "Windows async unreliable" claim — S9 / ADR-0017 §2026-07-18). On this transport `TransportWriter::send()` is an mpsc *enqueue*, so the actor's `SERIAL_SEND_TIMEOUT` bounds the enqueue, not the write. Owns the retained Windows-only `fix_write_timeout` DCB patch (`WriteTotalTimeoutConstant` 10 ms→5000 ms so CTS flow control has time to work; no Unix analog — Unix relies on the actor enqueue-fill `Wedged` backstop) and the DTR/RTS-before-flow-control open sequence. Frames are `;`-terminated only — no trailing `\r\n` (SPROG v1.4 FTDI buffer sensitivity, 2026-07-18). | inline `#[cfg(test)]` — 6 MERG codec tests |
| `transport/slcan_serial.rs` | Serial transport with SLCAN (Lawicel) framing. **Async** via `tokio_serial::SerialStream` (coexists with the blocking `gridconnect_serial` under the shared `TransportWriter` trait; keeps `tokio-serial` a dependency regardless of the gridconnect model — S9). | inline `#[cfg(test)]` |
| `transport/mock.rs` | Mock transport for unit and integration tests. Public module (S2, 2026-07-07) so tests in `lcc-rs/tests/**` can construct `MockTransport` / `MockTransportWriter`. `MockTransport::stall_handle() -> Arc<AtomicBool>` gives tests parametric control over writer stalls (used by the Transport Health seam's wedge/recovery tests). | — |

---

## Integration Boundaries

### Frontend → Backend IPC
- All commands return `Result<T, String>` (error as string)
- State accessed via `tauri::State<'_, AppState>`
- Events emitted: `lcc-node-discovered`, `lcc-message-received`, `lcc-connection-changed`, `cdi-read-complete`, `config-read-progress`

### Backend → lcc-rs API Surface
- `LccConnection` — main protocol orchestrator
- `TransportHandle` — broadcast inbound + mpsc outbound
- Types: `NodeID`, `EventID`, `NodeAlias`, `DiscoveredNode`, `SNIPData`, `ProtocolFlags`
- Protocol: `GridConnectFrame`, `MTI`, `DatagramAssembler`, `MemoryConfigCmd`
- Transport: `FrameEncoding` (`Standard` / `MergCanRs`) — passed to `GridConnectSerialTransport::open()`
- CDI: `Cdi`, `DataElement`, `EventRole`, `classify_event_slot()`, `walk_event_slots()`
- Transport: `LccTransport`, `GridConnectSerialTransport`, `SlcanSerialTransport`

### Lifecycle Ownership Transitions
- Discovery: lcc-rs probes → backend creates LiveNodeProxy actors → frontend receives events
- Config read: frontend orchestrator → backend batch reads → `lcc-rs/datagram_reader.rs` (`datagram_read_exchange`)
- CDI download: backend → `lcc-rs/discovery.rs` (`read_cdi_with_handle`) → `datagram_reader.rs`
- All datagram reads share: `MemoryReadConfig` (timeout, retry cap, post-ACK pacing delay). Config loaded from `tuning.toml` at app startup.
- Layout open: frontend orchestrator → backend hydrates snapshots → stores populated
- Sync apply: frontend orchestrator → backend writes changes → lcc-rs memory config

---

## Shared Conventions

### Key Generation
**Canonical implementation:** `app/src/lib/utils/editKey.ts`
- Format: `"${normalizedNodeId}:${space}:${address}"` (e.g., `"050201020300:253:100"`)
- `editKeyForLeaf(nodeId, space, address)` — single source of truth
- `parseEditKey(key)` — inverse
- Used by: `configChanges.svelte.ts`, `offlineChanges.svelte.ts`

### Slot Key Generation
**Canonical pattern (not a factory function):** `"${entry.node_key}:${entry.element_path.join('/')}"`
- Identifies a unique event slot across the frontend layers (bowtie, config sidebar, element picker)
- **Used for:** bowtie metadata edits (role classification), element tracking, ambiguous entry UI state
- **Consistency rule:** Wherever an `EventSlotEntry` is keyed in rendering (`{#each}` blocks, maps), the slot key must be computed identically and used in comparisons and state management. Inconsistent key computation leads to stale UI state across re-renders (regression: T037).
- **Proxy safety (ADR-0010):** Never compare object identity (`===`) of reactive-loop variables with `$state` proxies. Use the slot key string instead.
- **Used by:** `BowtieCard.svelte` (reclassifyKey state), `BowtieCatalogPanel.svelte`, `bowtieMetadata.svelte.ts`
- Anti-pattern: storing full `EventSlotEntry` object in `$state` and comparing by object identity — will fail on re-renders. Store the key string instead.

### Normalization Rules
**Canonical implementation:** `app/src/lib/utils/hexId.ts` + `lcc-rs/src/types.rs` (generic helpers); domain wrappers in `app/src/lib/utils/nodeId.ts` (`normalizeNodeId`, `formatNodeId`, `nodeIdToDisplayHex`, `nodeIdStringToBytes`) and `app/src/lib/utils/serialize.ts` (`parseEventIdHex`, `canonicalEventIdHex`, `formatEventIdHex`, `normalizeEventIdHex`).
- Node ID canonical: uppercase, no dots (`"050201020000FF"`)
- Node ID display: dotted-hex (`"05.02.01.02.00.FF"`)
- Event ID canonical: uppercase, no dots (`"05010101220000FF"`)
- Event ID display: dotted-hex (`"05.01.01.01.22.00.00.FF"`)
- `normalizeNodeId()` — removes dots, uppercases (string → string).
- `formatDottedHex()` / `formatCanonicalHex()` in `hexId.ts` — bytes → string. The byte-count rule lives here once; wrappers call it.
- `parseHexId(input, expectedBytes)` in `hexId.ts` — accepts dotted, contiguous, dashed, or space-separated input; returns `null` on length / non-hex failure.
- Anti-pattern: inline `.replace(/\./g, '')` without uppercase → lookup failures.
- Anti-pattern: inline `bytes.map(b => b.toString(16)...).join(...)` or `format!("{:02X}", b)` in a new site — always go through the helpers so the next ID width (12-byte UUID-like, etc.) is a one-line change.
- Governing doc: `product/architecture/naming-and-normalization.md`

### Formatting / Display
**Canonical implementation:** `app/src/lib/utils/formatters.ts`
- `formatConfigValue()` — Int→string, String→as-is, EventId→dotted hex (via `formatEventIdHex`), Float→.toFixed(2)
- `formatEventIdHex(bytes)` (re-exported from `serialize.ts`) — 8-byte array → dotted uppercase hex. The standalone `formatEventId` helper that previously lived here is gone; do not reintroduce it.
- `formatTreeConfigValue()` — resolves int enums to labels via mapEntries; EventId→dotted hex (via `displayEventIdHex`)

### Fallback Chains
**Canonical entry point:** `resolveNodeName(key: NodeKeyInput)` from `$lib/layout` (facade).
**Structured entry point:** `resolveNodeParts(key: NodeKeyInput)` from `$lib/layout` — returns `NodeDisplayParts { name, model, manufacturer, isUserNamed }` for UI rendering without baking manufacturer into the name.
**Implementation:** `app/src/lib/utils/nodeDisplayName.ts`
- Accepts `NodeKeyInput` (ADR-0010 sum type: live 12-hex or `placeholder:<uuid>`). Dispatches by variant.
- **Live nodes:** effective ACDI User Name leaf (space 251, draft→offline→baseline) → SNIP user_name → manufacturer+model → model → Node ID hex
- **Placeholder nodes:** effective ACDI User Name leaf → CDI `<identification>` manufacturer+model → model → placeholder key literal
- `resolveNodeName(key)` — facade function; returns a flat string. Still needed for sorting, search, and single-name-only contexts.
- `resolveNodeParts(key)` — facade function; returns structured parts for `<NodeLabel>` rendering. Edit-layer-aware. Manufacturer is never baked into `name`.
- `resolveNodePartsFromSnip(nodeId, node)` — SNIP-only structured resolver (no edit layer). Used internally by `effectiveLayoutStore` where circular-import prevents using `resolveNodeParts`.
- `resolveNodeDisplayName(nodeId, node)` — SNIP-only fallback tiers (internal to the composition, live nodes only)
- `resolveEffectiveUserName(tree, resolveValue)` — edit-layer tier (ADR-0003 point 4); pure, resolver injected
- **`NodeLabel.svelte`** (`$lib/components/NodeLabel.svelte`) — shared component for rendering node identity from `NodeDisplayParts`. Supports `orientation: 'inline' | 'compact'`. Inline = one line (group headers, wide contexts). Compact = two stacked lines (sidebar). Model is shown before manufacturer (`Model · Manufacturer`). No suppression rule: always shows product context when the node has a user-assigned name, even if user name happens to equal the model.
- Anti-pattern: calling `resolveNodeDisplayName` directly from components — use `resolveNodeName` or `resolveNodeParts` from `$lib/layout`
- Anti-pattern: inline fallback `node.user_name || nodeId` — use `resolveNodeName`
- Anti-pattern: displaying raw `hardwareRef.nodeKey` — always call `resolveNodeName(nodeKey)`
- Anti-pattern: baking node name into channel names (e.g. "TowerLCC-1 BOD A1") — channel names should stand alone; node context is rendered via `<NodeLabel>` in group headers or meta lines
- Anti-pattern: writing draft state back into `snip_data` (e.g. the removed `updateNodeSnipField`) — the snapshot is a hardware-reported mirror; drafts live in `configChangesStore`
- Governing docs: `product/architecture/naming-and-normalization.md`, ADR-0003, ADR-0010
- **Resolve late, on the consuming side.** Display names must be derived from
  the live `nodeInfoStore` at frontend derivation/render time, not consumed
  from a pre-baked upstream value. The config sidebar (`configSidebarPresenter`)
  and bowtie cards both do this. The Rust catalog's `EventSlotEntry.node_name`
  (`bowties-core/src/bowtie/catalog.rs` `node_display_name`) is computed once at
  build time and is a best-effort initial value only — `enrichEntryLabel`
  (`bowties.svelte.ts`) re-resolves it from `nodeInfoStore` so late SNIP arrival
  updates the card. Trusting the backing `node_name` directly is the regression
  this guards against.

### CDI File Naming
**Canonical saving:** `layout/io.rs` (layout dir), `commands/cdi.rs` (global cache)
- Cache key: `{sanitize(manufacturer)}_{sanitize(model)}_{sanitize(version)}`
- Global cache filename: `{cache_key}.cdi.xml` (in `app_data_dir/cdi_cache/`)
- Layout directory filename: `{cache_key}.cdi.xml` (in `layout.d/cdi/`)
- Legacy layout files may use `{cache_key}.xml` (without `.cdi` prefix) — readers fall back to this
- `cdi_cache_path_for_snapshot()` in `layout_capture.rs` — global cache path builder
- `get_cdi_path_for_snapshot()` in `layout/io.rs` — layout dir path builder (checks `.cdi.xml` then `.xml`)
- Anti-pattern: constructing layout CDI filenames from sanitized SNIP fields instead of `cache_key` — they use different extensions

### Config Change Layer Resolution
**Canonical implementation:** `app/src/lib/stores/configChanges.svelte.ts`
- `visibleValue(key)` — full resolution: draft → offlinePending → baseline (tree walk)
- `overrideValue(key)` — fast resolution: draft → offlinePending only (no tree walk)
- `_findLeaf` uses a `WeakMap`-backed address index (`addressIndexCache`) so repeated `visibleValue` calls into the same tree are O(1) instead of O(N) recursive walks. The cache is keyed by tree object identity; replaced trees get fresh indexes on next access.
- Anti-pattern: calling `visibleValue()` in bulk scans over `collectEventIdLeaves()` — the baseline tree walk is O(N) per leaf and the caller already has the leaf value.

### Bowtie Entry Enrichment
**Canonical implementation:** `app/src/lib/stores/bowties.svelte.ts`
- `enrichCardEntries(card)` — enriches all three entry arrays (producers, consumers, ambiguous) with `element_label` in one call.
- `enrichEntryLabel(entry)` — computes element_label from the live tree, falling back to `element_path.join('.')`.
- All preview build paths must use `enrichCardEntries` instead of mapping arrays individually — prevents the bug class where a new array is added or forgotten.
- Anti-pattern: calling `enrichEntryLabel` on producers and consumers but not ambiguous entries.

### Component Thin-Delegate Contract Tests
**Canonical example:** `app/src/lib/components/ElementCardDeck/SaveControls.test.ts` (`describe('Step 7 Option H: thin-delegate contract', ...)`)
- When a component is refactored to a thin delegate (presenter says yes/no → component invokes the `onAction` prop; no direct store mutation), the test suite MUST encode the contract directly:
  1. **Presenter-says-yes invokes prop.** Set up the minimum store state the presenter needs to gate `canX: true`, render the real component (not the presenter), click the button, and assert `onAction` was invoked. The Save no-op regression that motivated Option H slipped past mocked-presenter tests because the regression was in the component's parallel gate, not the presenter.
  2. **Component does NOT reach into cleanup stores.** Assert `.not.toHaveBeenCalled()` on `layoutStore.markClean`, `bowtieMetadataStore.clearAll`, `configChangesStore.commitForSave`, `offlineChangesStore.reloadFromBackend`, and any other post-action wiring that orchestrators own.
  3. **Mode parity.** When the component is mode-agnostic but the orchestrator wiring differs (online vs offline), parameterise the contract test over both modes with a `runFor(mode)` helper that mounts and unmounts cleanly.
- Anti-pattern: asserting that the component itself calls `commitForSave`/`markClean`/etc. — that test moves with the cleanup, not with the component, and inverts when the refactor lands.

## Skills

Skills that own specific workflow phases. Located in `.github/skills/`.

### design
**Files:** `SKILL.md`, `ASSESSMENT.md`, `SLICING.md`
**Purpose:** Feature-scoped architecture assessment and vertical slice planning. Runs after `/plan`, before `/slices`. Evaluates module depth, placement compliance, ADR compliance, and defines vertical slices with HITL/AFK labels.
**References:** `improve-codebase-architecture/LANGUAGE.md`, `grill-with-docs/ADR-FORMAT.md`, `grill-with-docs/GLOSSARY-FORMAT.md`

### slices
**Files:** `SKILL.md`, `SLICE-FORMAT.md`
**Purpose:** Generates slice-organized task file (`specs/<feature>/slices.md`) for cross-session progress tracking. Each slice has tasks with checkboxes, HITL/AFK labels, blocked-by relationships, and acceptance criteria.
**Output:** `specs/<feature>/slices.md`

### build
**Files:** `SKILL.md`, `tdd.md`, `deep-modules.md`, `interface-design.md`, `mocking.md`, `tests.md`
**Purpose:** TDD-first vertical implementation with multi-session support. Implements one slice at a time using red-green-refactor. AI judges session capacity and stops at slice boundaries. Includes pre-implementation checks and post-implementation enrichment.
**TDD methodology adapted from:** Matt Pocock's TDD skill
